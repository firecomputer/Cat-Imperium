# M10 실행 기록 (2026-08-29)

관전에서 나온 여섯 가지를 코드에서 추적해 고친 기록이다. 서로 독립된 여섯 개의
불만이 아니라 **네 개의 구조적 결함**이 겹친 결과였다.

---

# 0. 착수 전 진단

| # | 지적 | 코드상 원인 |
|---|---|---|
| 1 | 인물 사망이 너무 빠름 | 수명(42~74, 실측 평균 62)이 짧고 **배출 속도**가 겹쳤다. `POPULATION_PER_CHARACTER = 900000` 인데 국가 인구는 약 46,000 이라 턴당 0.05명. 실측 턴 120에 국가당 생존 3.2명, 고문석 7개 중 3.1개만 참 |
| 2 | 속국이 안 보임 | `_nation_color()` 가 `n.id` 로만 색을 만들고 `overlord` 를 안 봤다 |
| 3 | 승점 높아도 속국화 우선 | SUBJUGATION 전쟁은 warscore 35만 넘으면 땅을 한 뼘도 안 뺏고 끝났다. 100이어도 같았다 |
| 4 | 계속 파산 | 회생 경로가 없었다. **파산 유예 중에도 `printing_streak` 이 계속 올라** 타이머가 풀리는 턴에 즉시 재파산했다 (실측 434건 중 43건이 streak 26) |
| 5 | 강국이 파편을 흡수 안 함 | `military.gd` 가 제3국의 반란국 점령까지 즉시 회수해 `occupied_by_nation` 을 비웠다 → warscore 70점 점령항이 영영 0 → 전멸 아니면 백지평화 |
| 6 | 한번 반란난 지역이 계속 반란남 | `drift()` 에 감쇠항이 없고 문화 거리가 영구 양수. `p.integration` 은 행정부하에만 쓰였다 |

1·4·6은 한 고리다. 인물 풀이 마르면 `tax_efficiency` 0.7 바닥, `inflation_damping` 0,
`unrest_suppression` 0 → 인플레 → 불만 → 반란 + 화폐발행 → 파산 → 전 프로빈스
불만 +0.25 → 더 많은 반란. 실측 파산 434건 중 **430건이 프로빈스 1개짜리 잔존국**이었다.

---

# 1. 구현한 것

## 1.1 인물 풀 (`sim/systems/character_system.gd`)

```gdscript
const POPULATION_PER_CHARACTER := 360000.0   # 900000 에서
const LIFESPAN_BASE := 90.0                  # 42.0 에서
const LIFESPAN_HEALTH := 1.00                # 0.32 에서
const LIFESPAN_DEVIATION := 12.0             # 6.0 에서
```

수명 90 + 건강(0~100) → 90~190, 공공의료가 곱해져 최대 204. 실측 최대 235턴.

**정상상태 풀 = 배출률 × 수명이므로 두 상수는 함께 움직여야 한다.** 900000 을
그대로 두면 풀이 계속 3명이고, 200000 으로 내리면 33명이 되어 인재 선택이
무의미해지고 `world.characters` 가 300턴에 수만 개로 부푼다. 360000 에서 18.5명이다.

### 건국 코호트 분산

`initialize()` 가 12명 전부에게 `turn = 0` 을 줘서 한 세대가 통째로 죽었다
(실측 사망 histogram: 50대 2977, 60대 4291). `spawn_character` 에 `age_offset`
인자를 더하고 인덱스로 나이를 흩었다.

```gdscript
static func _founder_age(index: int) -> int:
	return int(LIFESPAN_BASE * float(index) / float(INITIAL_CHARACTERS_PER_NATION + 1))
```

**새 난수를 뽑지 않는다** — 인덱스만 쓴다 (§15). 수명이 145턴이 되면서 이 단계는
배율 조정보다 더 중요해졌다. 분산이 없으면 건국 12명이 전부 턴 120~190 사이에
죽어 관전 후반부에 전 세계 고문석이 동시에 비는 새 병목이 생긴다.

시장 가격(`market.gd` 의 `remaining = (death_turn - turn) / 30`)은 건드리지 않았다.
수명 145턴 중 마지막 30턴에만 값이 떨어지므로 매도 신호가 오히려 날카로워진다.

## 1.2 속국 색 (`view/map_renderer.gd`)

`_nation_color()` 가 realm 최상위를 본다. 중첩 속국은 금지되어 있어 한 홉이면 닿는다.

- 색상(hue)은 종주국과 **완전히 같고** 명도만 낮춘다 (`VASSAL_VALUE := 0.62`)
- 채움색이 같은 계열이 되어 속국 국경선이 안 보이므로 `lightened(0.15) → 0.45`

## 1.3 점령 vs 속국화 (`peace.gd`, `diplomacy.gd`)

```gdscript
const ANNEX_OVER_VASSAL_SCORE := 70.0   # peace.gd
const SUBJUGATION_MIN_TARGET := 3       # diplomacy.gd
```

warscore 70을 넘으면 속국화 분기를 건너뛰고 병합 분기로 떨어진다. 그 분기는 이미
땅을 먹고 남은 예산으로 잔존국을 속국으로 삼는다 — 압승은 "영토 + 잔존 속국"이 된다.
프로빈스 1~2개짜리 파편은 속국으로 삼지 않고 그냥 먹는다.

### 딸려 나온 버그

압승이 병합으로 끝나면 `EmpireSystem.on_peace` 가 여전히 SUBJUGATION 으로 정산해
**승자 권위 보상이 통째로 빠졌다** (`vassalize()` 가 이미 올렸다고 가정하는 분기).
`resolved_goal` 을 실제 `terms` 에서 판단하도록 고쳤다. `rump_vassal` 은 보상 없는
종속이므로 정복으로 정산된다.

## 1.4 파산 회생 (`credit.gd`, `nation.gd`)

```gdscript
const DEFAULT_DEBT_CEILING := 1.0     # 탕감 후 상한 = DEBT_ANCHOR × GDP × 이 값
const MILITARY_RECOVERY := 0.012
const DEFAULT_MEMORY_DECAY := 0.990   # 반감기 약 69턴
```

1. **`printing_streak` 을 파산 유예 중 동결.** 가장 큰 단일 버그였다. 발행 자체는
   계속 일어나므로 로그는 남기고 카운터만 얼린다.
2. **탕감에 상한.** 실측 재파산 시점의 `debt/gdp` 는 21.6 이라 65% 탕감 후에도
   7.5×GDP 가 남아 최저 신용도 이자(26.7%)가 세수(GDP의 12%)의 17배였다.
   탕감률 0.35는 그대로 두고 상한을 씌웠다.
3. **군사 페널티 회복.** 영구 ×0.5 였다 (2회 파산 0.25, 3회 0.125). 흑자를 내는
   동안 1.0 으로 돌아온다.
4. **신용 기억 감쇠.** `default_history` 는 통계용 카운터로 남기고 신용 산정은
   새 `default_memory` 가 맡는다. `credit_rating()` 호출 **전에** 감쇠시켜야
   같은 턴에 반영된다.

## 1.5 파편 흡수 (`military.gd`, `diplomacy.gd`)

즉시 회수 규칙은 **모국의 진압에만** 맞다. 제3국이 밟은 땅까지 회수하면 점령항이
0이 되어 조약으로 흡수할 길이 사라진다. 이미 있던 `Diplomacy.war_between()` 으로
반란전 여부를 보고 갈라진다. 제3국 전쟁이면 평범한 점령으로 남아 기존
`_annex_provinces` 경로가 흡수를 처리한다 — **새 흡수 코드는 만들지 않았다.**

`WAR_THRESHOLD` 는 건드리지 않았다. TUNING_M8 이 0.12 에서 100턴 이전 첫 파산
시드가 7/18 → 14/20 으로 뛴 것을 기록해 두었다. 대신 파편에만 붙는 가산을 뒀다:

```gdscript
const FRAGMENT_MAX_SIZE := 2
const FRAGMENT_APPETITE := 0.30
```

`edge` 는 0.75 에서 포화하므로 그 전에는 1프로빈스 파편과 어중간한 약소국이
똑같이 보였다.

## 1.6 반란 재발 (`unrest.gd`, `empire_system.gd`, `province.gd`)

```gdscript
const INTEGRATION_RELIEF := 0.80          # unrest.gd
const REINTEGRATION_INTEGRATION := 0.45   # unrest.gd
const ASSIMILATION_RATE := 0.010          # empire_system.gd
const ASSIMILATION_MAX_UNREST := 0.35
```

1. **integration 이 구조적 압력을 덜어 준다.** 문화·거리·월경지 항에만 곱한다.
   인플레·점령법은 그대로 둔다 — 통합된 본토라도 하이퍼인플레에는 흔들려야
   설계서 §13.4 의 "경제 고문의 죽음이 제국을 무너뜨린다"가 성립한다.
2. **진압된 땅은 부분 통합에서 다시 시작한다.** `spawn_rebellion` 이 봉기
   프로빈스의 integration 을 1.0 으로 올려놓기 때문에, 이 조치가 없으면 1번 때문에
   반란났던 땅이 오히려 가장 조용해지는 역전이 생긴다.
3. **완전 통합된 땅은 동화한다.** `p.culture` 는 그동안 어디서도 바뀌지 않아
   통합이 끝난 정복지도 `culture_distance` 가 남았고, 1번의 완화 0.8 을 뚫고
   잔여 20%가 계속 쌓였다. 새 `Province.assimilation` 이 조용한 턴을 누산하고
   1.0 에서 문화를 바꾼다. 불만이 오르면 되돌아간다 — 끝나는 타이머가 아니다.
   난수를 쓰지 않고, 소유가 바뀌는 두 곳에서 초기화한다.

---

# 2. 도중에 나온 것 — 프로빈스 목록 중복 버그

§3 의 조약 수확을 측정하다가 **인구 총합 보존이 3.4% 까지 깨지는 것**을 발견했다
(기준 <0.1%). 일회용 진단 스크립트로 첫 붕괴 턴을 잡았다.

```text
turn 67  pop_error 0.0576%
  nation 39 has DUPLICATE province 173   (×6)
  province 173 listed by 7 nations
  stale entries (owner_nation mismatch): 0
```

`n.provinces` 는 `p.owner_nation` 의 파생 색인이므로 집합이어야 하는데,
`Array.erase()` 는 **첫 항목만 지운다.** 한 번 중복이 생기면 영영 남고,
같은 프로빈스가 계속 다시 할양되며 늘어난다.

터진 곳은 `Economy._rebalance` 다. 중복된 프로빈스를 유출입 합계에는 여러 번
세면서 적용은 한 번만 해 `in_scale`/`out_scale` 이 틀어지고 인구가 무에서 생겼다.

```gdscript
while from.provinces.has(p.id):
	from.provinces.erase(p.id)
if not to.provinces.has(p.id):
	to.provinces.append(p.id)
```

`Unrest._transfer_province` 와 `Peace.annex` 두 곳에 같은 불변식을 넣었다.

**이 버그는 M10 이전부터 있었다.** M8.5 가 0.044%, M9.2 가 0.050% 로 기록한
"보존 오차"가 전부 이것이었다 — 영토 손바뀜이 늘수록 커지는 성질이라 그동안
임계 아래에 숨어 있었다. 고친 뒤 오차는 **정확히 0** 이다.

> **밸런스 다이얼을 돌리기 전에 불변식부터 확인할 것.** 이 버그는 §3 의
> 측정 자체를 오염시켰다 (§3.3).

---

# 3. 결과

## 3.1 인물 (`batch_characters` 20런 × 300턴)

| 지표 | M9.2 | M10 | 목표 | 판정 |
|---|---:|---:|---|---|
| 국가당 생존 인물 | 3.2 | **15.84** | 8~30 | PASS |
| 고문석 충원율 | 45% (3.1/7) | **88.8%** | ≥80% | PASS |
| 실현 수명 최대 | 98 | **235** | ≥200 | PASS |
| 실현 수명 평균 | 62.0 | **153.3** | — | — |
| 건국 세대 사망 턴 폭 | 사실상 0 | **103.4** | ≥60 | PASS |

**항목 1 해결.** 고문석이 차면서 `tax_efficiency`·`inflation_damping`·
`unrest_suppression` 이 설계값으로 돌아왔고, 그것이 아래 파산·반란 지표를 함께 끌었다.

## 3.2 파산 (`batch_sim` 20런 × 300턴)

| 지표 | M9.2 | M10 | 목표 | 판정 |
|---|---:|---:|---|---|
| 총 파산 | 434 | **156** | — | — |
| 재파산 비율 | 63% | **28%** (44/156) | ≤35% | PASS |
| streak 26 파산 | 43건 | **0건** | 0 | PASS |
| 첫 파산 턴 | 99.2 | 92.8 | 100~250 | FAIL |

파산 총량이 1/3 로 줄고 재파산 고리는 끊겼다. 첫 파산 턴은 오히려 조금 앞당겨졌다 (§4.1).

## 3.3 반란 (`probe_rebellion` 6런 × 300턴)

| 지표 | M9.2 | M10 | 목표 | 판정 |
|---|---:|---:|---|---|
| 진압 후 50턴 내 재반란 | 79% | **32%** | ≤40% | PASS |
| 진압 후 15턴 내 재반란 | 0% | 0% | 0% | PASS |
| 정부 진압률 | 47% | **36%** | 30~60% | PASS |
| 독립 인정률 | 53% | **55%** | 30~60% | PASS |
| 지속시간 중앙값 | 64턴 | 64턴 | 20~90 | PASS |
| 150턴+ 반란전 | 0% | 1% | <5% | PASS |
| 섬 반란 비중 | 30% | **4%** | — | — |
| 주둔 0 상태 반란 | 57% | 66% | ≤60% | FAIL |

**항목 6 해결.** 79% → 32% 다. 주둔 지표만 대역 밖이다 (§4.2).

## 3.4 전쟁·제국 (`batch_war` 20런 × 300턴)

| 지표 | M8/M9.2 | M10 | 목표 | 판정 |
|---|---:|---:|---|---|
| 영토 병합 | 61 | **190** | — | — |
| 반란 상실 : 병합 | 11.5 : 1 | **2.2 : 1** | — | — |
| 속국화 | — | 85 | — | — |
| 제국 (share ≥ 0.12 + 속국) | 0 | **3 / 20런** | ≥1/런 | FAIL |
| 최대국 세계 GDP 점유 | 6.6% | 7.4% | <70% | PASS |
| 국경 덩어리 | 1.30 | 1.50 | 현 수준 | 주의 |

**제국이 처음으로 생겼다** (peak realm_share 13~14%, 지속 86~102턴). 런당 0.15개로
목표에는 못 미친다. 원인 분석은 §4.3 에 측정과 함께 남긴다.

## 3.5 회귀

| 지표 | M9.2 | M10 | 목표 | 판정 |
|---|---:|---:|---|---|
| 인구 총합 보존 오차 | 0.050% | **0.000000%** | <0.1% | PASS |
| 세계 도시 수 | 26.6 | 28.9 | 15~40 | PASS |
| 문화별 생존율 | 100% | 100% | 각 ≥10% | PASS |
| 최대국 GDP 점유 | 6.6% | 7.4% | <70% | PASS |
| 최고 인프라 | 8.61 | **9.72** | 7~9 | FAIL (신규) |
| 최대 국가 부채/GDP | — | 35.2 | <10 | FAIL (기존) |

## 3.6 테스트와 결정론

```text
character · credit · empire · laws · market · military · naval · rebellion · view · war  전부 PASS
```

`seed 1 × 300턴` 을 두 번 돌려 세 파일 모두 바이트 동일했다. 새 기준선:

```text
sim           150ebea1955ec4695ef1774f4d0e98c5968aac91a1fe809945b03e25d1f3a12a
defaults      102bc5822f7a7ef405f98188d3445ffff67630429fe884f2b5f47e8e346d5a7d
credit events 72a7bea37c10f7a0032969b9936757aaf6978b1a98048a54c97b1e43f5313575
```

M9.2 해시는 폐기한다. 새 난수 스트림도, 기존 스트림의 뽑는 횟수 변경도 없다 —
코호트 분산과 문화 동화는 둘 다 인덱스·누산기 기반이다.

### 고친 기존 테스트 셋

셋 다 **옛 버그를 규격으로 굳혀 둔 단언**이었다.

- `test_credit.gd` — 파산 유예 중 `printing_streak == 1` 을 기대했다. 이제 0 이다.
- `test_rebellion.gd` / `test_war.gd` — 프로빈스 기본 `integration` 이 1.0 이라
  구조적 drift 가 거의 사라졌다. 두 테스트가 재봉기시키려는 땅은 의미상
  **막 정복한 미통합 변경지**이므로 `integration = 0.0` 을 명시했다.

### 도구

`analyze_characters.py` 는 건국 스냅샷만 봤다 — 그래서 턴 60 이후 풀이 3.2명까지
마르는 것을 한 번도 못 잡았다. 종료 시점의 풀 크기·고문석 충원율·수명 최대·
건국 세대 사망 폭을 실제 PASS/FAIL 기준으로 넣었다.

같은 파일의 분모 버그도 고쳤다: 반란국까지 세어 초기 12명/7석 검사가 반란이 난
시드에서 무조건 FAIL 이었다. 건국 국가만 분모로 센다.

---

# 4. 대역 밖 항목과 원인

## 4.1 첫 파산 턴 92.8 (목표 100~250)

파산 총량은 434 → 156 으로 줄었지만 **가장 이른 파산의 시점은 오히려 조금
앞당겨졌다.** 파산의 절대다수는 여전히 프로빈스 1~2개짜리 잔존국이고, 이들은
세수 기반이 구조적으로 부족해 인물·신용 개선이 닿지 않는다. §1.4 의 네 조치는
전부 "파산한 뒤의 회생"을 고치지, 그 잔존국이 처음 무너지는 시점을 늦추지 않는다.

같은 뿌리에서 최대 국가 부채/GDP 35.2 (기준 <10) 도 나온다 — 영토를 잃어 GDP 가
무너져도 `debt` 는 명목값 그대로다. §1.4-2 의 상한은 **파산 시점에만** 걸리므로
파산 전까지 비율이 커지는 것을 막지 못한다.

## 4.2 주둔 0 상태 반란 66% (목표 ≤60%)

통합 완화로 불만이 `GARRISON_NEED_MIN`(0.35)을 넘는 프로빈스가 줄어 **주둔 대상
자체가 줄었다.** 치안 비율은 0.4~1.2% 다. 반란은 이제 상시 압력이 아니라
드물게 터지는 사건이라, 문턱을 그대로 두면 아무도 미리 주둔하지 않는다.
`GARRISON_NEED_MIN` 을 낮추는 것이 다음 후보지만, M8.5 가 이 값 하나로 배정과
해제를 다 판단하게 해 둔 구조(7턴 진동, TUNING_M9 §19-4)를 먼저 손봐야 한다.

## 4.3 제국 3/20런 (목표 ≥1/런) — 조약 수확은 다이얼이 아니었다

가설은 "조약당 수확이 0.73 프로빈스라 정복이 반란 상실을 못 따라잡는다"였다.
`COST_PROVINCE_BASE`/`COST_PROVINCE_VALUE` 를 내려 측정했다.

**처음 측정(중복 버그 있는 상태)** 은 비용이 지배 변수처럼 보이게 했다:

| base / value | 영토 병합 | 최대 영토 | 최대국 GDP 점유 | 제국 |
|---|---:|---:|---:|---:|
| 8 / 35 | 166 | 17 | 7.4% | 3 |
| 4 / 16 | 346 | 23 | 8.8% | 2 |
| 2 / 8 | 579 | 40 | 19.2% | 5 |

**§2 의 버그를 고친 뒤 다시 재면 그 효과가 사라진다:**

| base / value | 영토 병합 | 최대 영토 | 최대국 GDP 점유 | 제국 |
|---|---:|---:|---:|---:|
| **8 / 35 (유지)** | 190 | 15 | 7.4% | 3 |
| 2 / 8 | 701 | 15 | 7.4% | 2 |

`realm_value` 와 `realm_province_count` 가 중복 프로빈스를 그대로 세고 있었다.
비용을 내릴수록 할양이 잦아지고, 할양이 잦을수록 중복이 쌓여, **제국이 커진 것처럼
보였을 뿐이다.**

비용을 1/4 로 내리면 병합 횟수만 3.7배가 되고 최대 영토는 15로 같다. 땅이 더 빨리
손바뀜할 뿐 아무도 쌓지 못한다 — 싸게 뺏을 수 있으면 남도 싸게 뺏어 간다.
설계서 §12.2 의 "영토 할양 = 비용 높음"을 유지하고 **8/35 를 그대로 둔다**
(설계서 §12.2.1 에 측정과 함께 기록했다).

**진짜 원인은 정복이 아니라 유지다.** 2/8 에서 병합 701 > 반란 상실 362 인데도
최대 영토가 15에 머문다. 얻은 땅을 반란이 아니라 **다른 정복자에게** 다시 잃는다.
다음에 볼 곳은 개전 빈도(`WAR_THRESHOLD` 0.25)와 휴전(`TRUCE_TURNS` 20)·동맹이
승자를 얼마나 오래 보호하는지다.

## 4.4 최고 인프라 9.72 (목표 7~9)

항목 1의 직접적 결과다. 기술 고문석이 차면서 `infra_cost_mult` 가 설계값
(1 - tech×0.30)으로 내려갔고 건설이 싸졌다. 고문 효과가 처음으로 제대로 작동하는
상태라 이 값이 새 정상이다 — 대역을 옮길지 `Economy` 앵커를 조일지는 튜닝 판단이다.

---

# 5. 남은 것

1. **정복지 유지** (§4.3) — 대제국의 진짜 병목. 개전 빈도·휴전·동맹 쪽이다.
   조약 수확은 아니라는 것을 측정으로 배제했다.
2. 잔존국의 첫 파산 시점과 파산 전 부채/GDP 폭주 (§4.1).
3. `GARRISON_NEED_MIN` 과 배정/해제 이력 (§4.2, TUNING_M9 §19-4 와 같은 뿌리).
4. 최고 인프라 대역 재설정 (§4.4).

---

# M10.1 동맹 만료 (2026-08-29)

M10 §4.3 이 "다음에 볼 곳"으로 남긴 개전 빈도를 실제로 파고든 기록이다.
**측정 → 가설 기각 → 다시 측정**을 세 번 반복해서 진짜 원인에 닿았다.

## 6. 원인 추적

일회용 진단 스크립트(`tools/probe_empire.gd`, 작업 후 삭제)로 네 단계를 쟀다.

### 6.1 정복지는 반란으로 사라진다 — 다른 정복자가 아니다

정점을 찍은 뒤 그 런의 최대 국가가 잃은 땅 (6런 × 300턴):

| 원인 | 건수 |
|---|---:|
| 반란 | **16** |
| 조약으로 뺏김 | 1 |

### 6.2 큰 나라는 정상 상태에서 이미 안정적이다

턴 200, 프로빈스 10개 이상 국가의 모든 땅에 걸린 턴당 압력 (401개 표본):

```text
occ +0.00102   infl +0.01797   cult +0.00030   dist +0.00175
excl +0.00045  over +0.00023   gar -0.00400
supp -0.01282  coh -0.01205    합계 -0.00715   평균 integration 0.98
```

**합계가 음수다.** M10 §1.6 의 통합 완화가 작동해 문화 항이 0.0003 까지 죽었다.
문제는 전체 영토가 아니라 **갓 정복한 땅**뿐이다 (integration ≤ 0.30, 4개 표본):

```text
occ +0.04500  ← 나머지 전부를 합친 것보다 크다
cult +0.01403   dist +0.00912   gar -0.02994
합계 +0.01397   평균 integration 0.09
```

불만 0.15 → 1.0 이 60턴, 통합 0 → 1 이 93턴. **경주에서 불만이 이긴다.**

### 6.3 왜 다들 약탈 점령법을 드는가 — 법 AI가 정복을 모른다

턴 200 점령법 분포: 큰 나라 **35/37 이 약탈**(severity 0.9), 작은 나라 209/218.

`LawEvaluator.evaluate` 를 desperation `d` 에 대해 풀면

```text
pillage(d)      = -0.40 + 5.28d
assimilation(d) = +0.35 - 0.83d      교차점 d = 0.123
```

실측 desperation 은 0.42 다. **0.12만 넘으면 약탈이 무조건 이긴다.**
`immediate_income * (1 + d*4)` 배율이 압도적인데, 정작 **그 나라가 이문화 땅을
얼마나 들고 있는지는 평가식에 없다.** 정복해도 법 AI가 눈치채지 못한다.

조치: `Nation.foreign_exposure`(이문화 프로빈스 비율)를 `_recompute_administration`
의 기존 루프에서 함께 계산하고, 점령 카테고리에 한해 비용을 그 노출에 비례해 매긴다.

```gdscript
const OCCUPATION_EXPOSURE_W := 8.0
if law.category == "occupation":
	score -= law.severity * n.foreign_exposure * OCCUPATION_EXPOSURE_W
```

효과는 있으나 작다 (반란 상실 424 → 367). 계수를 25로 올려도 29/38 이 그대로
약탈이다 — **땅이 동화되고 나면 노출이 0 이 되고, 그때는 약탈이 실제로 공짜다**
(occ 항은 이문화 땅에만 걸린다). 모델이 자기모순 없이 맞물려 있어 이 레버는 여기까지다.

### 6.4 진짜 병목은 영구 동맹이었다

최대 영토가 어떤 설정에서도 15로 고정됐다 (8/35, 2/8, W=8, W=25). 개전 관문을 쟀다:

```text
total            70765
eligible         41804      표적 못 찾고 끝난 국가-턴  41644 (99.6%)
t_ally           49187  ←   동맹이 지운 표적
t_candidate       3455      모든 관문을 통과한 (국가, 표적) 쌍
실제 선전포고       241
```

**동맹이 살아남는 표적보다 14배 많은 표적을 지운다.** TUNING_M8 이 같은 병목을
보고 `ALLY_MIN_THREAT` 를 0.70 으로 올렸지만 소용없던 이유가 그 문서에 이미 적혀
있다 — **동맹에 해지가 없다.** 한 번만 임계를 넘으면 영구 동맹이고 그 이웃은
영영 공격 대상이 못 된다. 진입 임계는 다이얼이 될 수 없었다.

## 7. 구현 — 동맹 임기와 재심사 (설계서 §11.4)

```gdscript
const ALLIANCE_TERM := 80      # diplomacy.gd
var alliance_expiry: Dictionary = {}   # nation.gd, nation_id -> 재심사 턴
```

만료는 해지가 아니라 **형성 조건 재심사**다.

| 상황 | 결과 |
|---|---|
| 둘 다 두려워하는 제3국이 아직 있다 | 갱신, 임기 재설정 |
| 공동 위협이 사라졌다 | 해지 (`alliance_ended` / `term_expired`) |
| 어느 쪽이든 교전 중 | 갱신 — 참전 중 이탈은 배신이고 §11 에 규격이 없다 |
| 동맹국 소멸 | 즉시 정리 (`ally_dead`) |

쌍마다 한 번만 심사하도록 id 가 낮은 쪽에서 처리한다 (§15). 새 난수는 없다.
`EmpireSystem.vassalize` 가 동맹을 끊을 때 만료 기록도 함께 지운다.

## 8. 결과 — 임기는 전쟁과 파산을 맞바꾼다

20런 × 300턴:

| ALLIANCE_TERM | 선전포고 | 영토 병합 | 속국화 | 최대국 GDP 점유 | 첫 파산 턴 | 총 파산 |
|---|---:|---:|---:|---:|---:|---:|
| 없음 (영구) | 688 | 190 | 95 | 7.4% | 92.8 FAIL | 156 |
| 40 | 1278 | 391 | 219 | 7.8% | 77.9 FAIL | 293 |
| **80 (채택)** | 1058 | 300 | 284 | **9.5%** | **104.0 PASS** | 241 |

임기가 짧을수록 개전 창이 자주 열리고 전시 군사비(×1.5)가 상시화되어 파산이
빨라진다 — TUNING_M8 이 `WAR_THRESHOLD` 를 내렸을 때 본 것과 같은 교환이다.
80 은 개전을 54% 늘리면서 **첫 파산 턴이 이 마일스톤에서 처음으로 목표 대역에
들어온** 유일한 값이다.

### 회귀 (ALLIANCE_TERM 80)

| 지표 | M10 | M10.1 | 목표 | 판정 |
|---|---:|---:|---|---|
| 첫 파산 턴 | 92.8 | **104.0** | 100~250 | **PASS** (신규) |
| 재파산 비율 | 28% | 29% | ≤35% | PASS |
| 인구 총합 보존 | 0.000000% | 0.000000% | <0.1% | PASS |
| 국가당 생존 인물 | 15.84 | 15.22 | 8~30 | PASS |
| 고문석 충원율 | 88.8% | 86.1% | ≥80% | PASS |
| 진압 후 50턴 내 재반란 | 32% | **14%** | ≤40% | PASS |
| 정부 진압률 | 36% | 31% | 30~60% | PASS (경계) |
| 독립 인정률 | 55% | 60% | 30~60% | PASS (경계) |
| 주둔 0 상태 반란 | 66% | 72% | ≤60% | FAIL |
| 총 파산 | 156 | 241 | — | 주의 |
| 제국 수 | 3/20런 | 2~3/20런 | ≥1/런 | FAIL |

테스트 10종 전부 PASS. `test_war.gd` 에 만료·갱신 회귀를 추가했다.

### 결정론 (새 기준선)

```text
sim           8937633c32d499177ae387d366bb0851dec7c3f51c7ab61a960495d16f2986bb
defaults      3d81857a38fbcf4386376ccc1291c9539a6c4e58107eb51c93ffed73f1bbae77
credit events 4cc0f2d30224e6ee0988e9e94674ace86f0e043b04dec91e524925d73765220f
```

## 9. 남은 것

1. **제국은 여전히 안 선다.** 동맹 만료가 개전을 54% 늘리고 병합을 58% 늘렸지만
   반란 상실이 367 → 700 으로 같이 뛰어 순증이 안 난다. 병목이 "정복 기회"에서
   **"정복지 유지"** 로 넘어갔다 — §6.2 의 경주(불만 60턴 vs 통합 93턴)가 그대로 남는다.
   `INTEGRATION_RATE` 0.020 → 0.045 를 시험하면 반란 상실 751 → 685, 최대 영토
   17 → 19 로 움직이지만 제국 수는 그대로다. 반란 대역을 흔들므로 이번에는 되돌렸다.
2. 진압률 31% / 독립률 60% 가 대역 양 끝에 붙었다. 전쟁이 늘면서 모국이 반란
   진압에 쓸 여력이 줄어든 몫이다.
3. 주둔 0 상태 반란 72% (M10 66%). 같은 원인 — 야전군이 대외전쟁에 묶인다.
4. 총 파산 156 → 241. 첫 파산 턴은 대역에 들었지만 절대량은 늘었다.
5. 배신(동맹국 공격)과 자발적 파기는 아직 규격이 없다 (설계서 §11.4 말미).
