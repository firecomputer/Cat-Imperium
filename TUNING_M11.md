# M11 — 계측 + 관측 창 (게이트)

> **범위**: 2차 계획서 `CAT_EMPIRE_PHASE2.md` §M11 (M11.1 / M11.2 / M11.3) + §M17 의
> M3 앵커 병행 처리.
> **결론 한 줄**: 지표는 전부 측정 가능해졌고 유령값은 0 이 됐다. **게이트는 중간 판정** —
> 제국은 서지만(20런 중 5런) 대역(60%)에 못 미친다. **M15 투자층은 보류**다.

측정 기준선: 현행 노이즈 지도, 20런 × 600턴, 시드 1~20.
`out/m11_600b.csv` 및 `_defaults / _credit_events / _nations / _empires` 부산물.
(`out/m11_600.csv` 는 §4.4 `Economy.aggregate` 수정 *이전*의 기준선이다 — 비교용으로 남긴다.)

```
godot --headless --script res://tools/batch_sim.gd -- --runs 10 --turns 600 --seed0 1  --out res://out/m11_a.csv
godot --headless --script res://tools/batch_sim.gd -- --runs 10 --turns 600 --seed0 11 --out res://out/m11_b.csv
python tools/analyze.py out/m11_600b.csv
tools/run_tests.sh
```

---

## 0. 측정 전에 고친 것

계획서 §M17 이 "M3 앵커 초과는 불변식이므로 M11 과 병행 처리한다" 고 못박았고,
§0 의 결정에 따라 **수정을 먼저 하고 한 번만 측정**했다.

### 0.1 M3 GDP 앵커 초과 11샘플 — 불변식 위반이 아니었다

프로브(5런 × 600턴)로 초과 96샘플의 원인을 갈랐다.

| 원인 | 건수 |
|---|---:|
| 생산성 법률 변경으로 상한이 내려감 | 64 |
| 그 감쇠의 꼬리 (샘플 간격 10턴) | 29 |
| 소유국 변경 | 3 |

전형적인 표본이다.

```
gdp_pc=3782.4  cap=3659.3  prev_cap=4065.9  infra=10.00  terrain=1.00  unrest=0.00
```

**절대 구조 상한 4896 은 한 번도 넘지 않았다** (최대 관측 3822). 즉 §4.1 의
"인프라가 1인당 GDP 의 하드 상한을 정한다" 는 지켜지고 있었고, `batch_sim` 의
판정식이 *설계된 과도 상태*를 위반으로 세고 있었다. `DECLINE_RATE 0.18` 은
"하락은 빠르되 점진적" 이라는 설계이므로, 법률이 바뀐 순간 `gdp_pc` 가 계단식으로
잘려야 한다는 판정이 오히려 설계와 충돌한다.

두 번째 함정도 있었다. 앵커를 **턴이 끝난 뒤** 다시 계산하면 파이프라인 5단계
(`tick_production`)가 실제로 본 값이 아니라 11단계(`Unrest`)에서 더 오른 불만이
반영된 낮은 값이 나온다. 그 차이만으로 300턴 2런에서 26~40건의 가짜 위반이 잡혔다.

**조치**

- `Economy.province_anchor(p, n)` 로 앵커 식을 한 곳에 모았다. 식이 `economy.gd` 와
  `batch_sim.gd` 에 복제돼 있던 것이 이 혼선의 뿌리다
- `Province.anchor_gdp_pc` 에 생산 틱이 쓴 앵커를 남긴다. 계측은 이 값으로만 잰다
- 판정을 **"앵커 위에 있으면서 `gdp_pc` 가 상승했는가"** 로 바꿨다. 앵커가 내려가서
  생긴 초과는 감쇠이지 위반이 아니다
- 과도 상태는 버리지 않고 `anchor_over_turns_max` 열로 추적한다

**결과: 위반 0 (20런 × 600턴). PASS.**
과도 최장 지속은 224턴 — 인프라가 `INFRA_MAX` 에 붙은 뒤 불만만 계속 오르면
앵커가 끝없이 내려가 `gdp_pc` 가 영구히 뒤쫓기 때문이다. 위반은 아니나 §6.6
인프라 포화와 같은 뿌리이므로 M17 에서 함께 본다.

### 0.2 유령값 — `unrest` 와 `trade`

전 법률 `.tres` 의 힌트 키 15종을 소스 전수 대조했다. 두 개가 유령값이었다.

| 키 | 선언 | .tres | 시뮬 소비처 | AI 소비처 |
|---|---|---:|---|---|
| `unrest` | `Law.ADDITIVE` (시뮬 스탯) | 12개 법 | **없음** | `LawEvaluator` |
| `trade` | `Law.ADDITIVE` (시뮬 스탯) | 3개 법 | **없음** | `LawEvaluator` |

§5.2 의 `immediate_income` 보다 나쁜 형태다. 그것은 `AI_HINTS` 로 선언된 힌트였지만
이 둘은 **시뮬 스탯이라고 선언해 놓고 아무도 읽지 않았다.** 점령법 3종이 전부
`unrest` 값을 갖는데 실제 불만은 `severity` 를 통해서만 들어갔고, 교역법 3종의
`trade` 는 아예 아무 데도 닿지 않았다.

**조치 — 삭제가 아니라 구현.**

```gdscript
# unrest.gd — drift 에 항 추가
const LAW_UNREST_W := 0.15
d += n.law_modifier("unrest") * LAW_UNREST_W
```

법 합산 `unrest` 는 전형적으로 ±0.2 이므로 턴당 ±0.03 이 된다. 기존 문화 거리
(`CULTURE_W` 0.03)·주둔(`GARRISON_W` 0.04)과 같은 자릿수다. 원값(±0.12/턴)을
그대로 더하면 약탈법 하나로 8턴 만에 봉기해 세계가 즉시 조각난다.

```gdscript
# economy.gd — 앵커에 교역 항 추가
const TRADE_W := 0.3
const TRADE_INLAND_SHARE := 0.3
static func trade_mult(p, n) -> float:
	var share := 1.0 if p.is_coastal else TRADE_INLAND_SHARE
	return maxf(1.0 + n.law_modifier("trade") * TRADE_W * share, TRADE_MULT_MIN)
```

교역은 바다로 한다. 해안 프로빈스가 이득의 대부분을 가져가고 내륙은 30% 만 받는다 —
§5.1 이 요구한 "전역 다이얼이 아니라 나라마다 다른 값" 을 지리가 제공하는 형태다.
M13 실지도 교체 뒤 이 항의 의미가 커진다.

### 0.3 회귀 테스트 `test_law_hints.gd`

손으로 관리하는 목록이 아니라 **소스를 직접 훑어** 소비처를 찾는다. 소비처가
사라지면 테스트가 깨진다.

- `.tres` 의 모든 키가 `MULTIPLICATIVE ∪ ADDITIVE ∪ AI_HINTS` 에 선언돼 있는가
- 시뮬 스탯 키에 `sim/` 소비처가 있는가 (없으면 유령값)
- 힌트 키가 `sim/ai/` 에서만 읽히는가 (시뮬에 새면 힌트가 아니다)
- 선언만 되고 쓰는 법이 없는 죽은 키가 없는가

`Nation.law_modifier` 자체는 통로일 뿐이므로 소비처 집계에서 제외한다.

```
godot --headless --script res://tools/test_law_hints.gd     # PASS (0 위반)
```

---

## 1. M11.1 — 미측정 지표 4종

전부 `batch_sim.gd` 가 산출하고 `analyze.py` 가 판정한다.

| 지표 | 목표 | 측정 (20런 × 600턴) | 판정 |
|---|---|---:|---|
| 1인당 GDP 지니 (국가 간) | 0.30~0.50 | **0.190** | FAIL |
| 1인당 GDP 지니 (국가 내) | 0.20~0.40 | **0.084** | FAIL |
| 전쟁 중 평균 보급률 | 0.60~0.80 | 0.790 | PASS |
| 파산 후 50턴 생존율 | 0.40~0.60 | **0.988** | FAIL |
| 파산 후 100턴 생존율 | 0.40~0.60 | **0.962** | FAIL |
| 월경지 보유국 평균 수명 | 본토형보다 짧을 것 | 583.1 vs 589.4 | PASS (거의 동률) |

### 1.1 지니계수 — 단순 2지표

국가 간과 국가 내를 합산하지 않는다. §14 의 두 상품이 각각 다른 축을 파므로
중첩항까지 분해할 필요가 없다.

- **국가 간**: 국가별 1인당 GDP 를 인구로 가중한 지니 (국채가 먹는 축)
- **국가 내**: 국가마다 프로빈스 지니를 낸 뒤 인구 가중 평균 (프로빈스 지분이 먹는 축)

**가장 중요한 발견 — 세계가 시간이 갈수록 평평해진다.**

| 턴 | 0 | 100 | 200 | 300 | 400 | 500 | 600 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 지니 (국가 간) | 0.219 | 0.315 | 0.257 | 0.216 | 0.186 | 0.174 | 0.190 |
| 지니 (국가 내) | 0.318 | **0.407** | 0.303 | 0.183 | 0.123 | 0.097 | **0.084** |

둘 다 **100턴에 정점을 찍고 이후 단조 감소**한다. 인프라가 `INFRA_MAX = 10` 에
포화하면 앵커가 인프라의 함수이므로 모든 프로빈스가 같은 1인당 GDP 로 수렴한다.
300턴 최고 인프라 10.00(20런 전부)이 그 증거다.

> **이것은 튜닝 문제가 아니라 구조 문제다.** 편차의 원천이 인프라 하나뿐이고
> 인프라에 상한이 있으면, 시간은 반드시 편차를 지운다. §14 의 세 상품은 모두
> 편차를 먹고 사는데 세계가 편차를 스스로 없앤다.

### 1.2 파산 후 생존율 98% — §4.3 "나라가 죽지 않는다" 의 정량

파산 1845건 중 50턴 뒤에도 살아 있는 것이 98.8%. **파산이 죽음으로 이어지지 않는다.**
600턴 동안 건국국가 799개 중 죽은 것은 36개(4.5%)뿐이다. 1차 §4.3 이 300턴에서
"39.2/40 생존" 으로 관측한 것이 600턴에서도 그대로다 — 관측 창을 두 배로 늘려도
건국국가는 죽지 않는다.

### 1.3 월경지 수명 — 지표 정의를 고쳐야 했다

전 국가로 재면 월경지 보유국이 **더 오래** 산다(280.9 vs 135.8턴, 300턴 런). 반란
신생국 1907개가 1프로빈스로 태어나 평균 167.8턴에 죽고 월경지를 가질 일이 없어
본토형 표본을 오염시키기 때문이다. **건국국가(`birth_turn == 0`)로 자른다.**

건국국가 기준 583.1 vs 589.4턴 — 방향은 맞지만 차이가 1.1%뿐이다. 둘 다 관측 창
600턴에 절단돼 있어(사망 4.5%) 사실상 "둘 다 안 죽는다" 는 말이다.
**§1.2 가 해결되기 전에는 이 지표가 의미를 갖지 못한다.**

### 1.4 보급률 0.792 — 유일한 PASS, 그러나 대역 상단

야전군(주둔 분견대 제외) 기준. 대역 0.6~0.8 의 위쪽 끝에 붙어 있다. 보급이
전쟁의 제약으로 거의 작동하지 않는다는 뜻이므로 M13 실지도(거리·지형이 실제로
멀어짐) 뒤 재측정이 필요하다.

---

## 2. M11.2 — 600턴 표준 런

### 2.1 성능

| 항목 | 값 |
|---|---|
| 600턴 1런 (계측 추가 전) | 15.8초 |
| 600턴 10런 (계측 추가 후) | 225초 |
| **20런 총** | **약 7.5분** |

계획서가 우려한 성능 문제는 없다. 배치 런 수를 12로 줄일 필요도, 병렬화도 불필요.
단 `Tool` 실행 시간 제한 때문에 10런씩 두 번 돌려 합쳤다 (시드 1~10 / 11~20).
결정론은 유지된다 — 같은 시드로 두 번 돌려 5개 CSV 전부 바이트 동일 확인.

제국 추적이 `EmpireSystem.realm_share` 를 국가마다 부르면 프로빈스를 국가 수만큼
다시 훑어 시뮬보다 비싸진다. 프로빈스 1회 순회로 realm 별 가치를 모으도록 바꿨다
(`_realm_values`).

### 2.2 100턴 단위 스냅샷

| 턴 | 실효 국가 | 전체 국가 | 최대 영토 | 최대 GDP 점유 | 제국 | 지니(간) |
|---:|---:|---:|---:|---:|---:|---:|
| 0 | 39.1 | 40.0 | 12.2 | 0.067 | 0.00 | 0.219 |
| 100 | 37.8 | 39.8 | 15.4 | 0.094 | 0.00 | 0.315 |
| 200 | 32.0 | 47.4 | 16.4 | 0.082 | 0.00 | 0.257 |
| 300 | 31.0 | 55.1 | 19.6 | 0.084 | 0.00 | 0.216 |
| 400 | 30.4 | 60.9 | 21.1 | 0.086 | 0.05 | 0.186 |
| 500 | 29.8 | 66.3 | 21.1 | 0.084 | 0.05 | 0.174 |
| 600 | 29.5 | 69.8 | 21.3 | 0.097 | 0.10 | 0.190 |

**전체 국가 수가 40 → 70 으로 늘어난다.** 반란 독립국이 계속 태어나기 때문이다
(600턴 시점 평균 57.6개가 반란국). 실효 국가(프로빈스 ≥ 3)는 39.1 → 29.5 로
줄지만 **400턴 이후 사실상 정지**한다 (30.4 → 29.5, 200턴 동안 0.9 감소).

실효 국가 잔존률 **75.5%** — 목표 40~70% 를 살짝 넘겼다. 1차의 98%(300턴, 전 국가
기준)와 비교하면 크게 개선됐으나 이는 대부분 **실효 국가 필터의 효과**이지
세계가 통합돼서가 아니다.

### 2.3 제국 생애사 — 300턴 관측 창이 실제로 제국을 가리고 있었다

| | 값 |
|---|---|
| 에피소드 | 3건 / 3런 (20런 중 **15%**) |
| 유지 24턴 이상 (M12 정의) | 3건 / 3런 (15%) |
| 존속 턴 | 중앙값 124, 최대 158 |
| 최대 realm 점유 | 0.164 |
| 해체율 | 2/3 = **66.7%** (대역 40~70% 안) |
| 해체 원인 | 점유율 하락 2, 관측 창 끝 1 |

```
seed nation enter exit  dur  peak     reason
10   24     350   397   47   0.13073  realm_share_below_threshold
12   28     365   489   124  0.12987  realm_share_below_threshold
13   15     442   600   158  0.16352  horizon
```

> **3건 전부 350턴 이후에 시작한다.** 가장 이른 것이 350턴.
> 1차가 300턴에서 관측을 끊은 것은 제국이 서기 시작하는 바로 그 지점이었다.

계획서 §6.1 이 "제국이 안 선다" 로 본 것에는 **관측 창 문제가 실제로 섞여 있었다.**
다만 60% 대역에는 크게 못 미치므로 **관측 창만의 문제는 아니다.**

> ⚠️ **이 수치는 20런에서 노이즈에 묻힌다.** §4.4 의 `Economy.aggregate` 수정 전에는
> 7건/5런(25%)이었다. 형성 런이 5 → 3 으로 줄었으나 n=20 에서 25% 와 15% 는
> 통계적으로 구분되지 않는다(표준오차 약 9%p). 게이트 판정은 어느 쪽이든 같다.
> **M13 재측정에서는 런 수를 늘려야 이 지표로 판정할 수 있다.**

## 3. 게이트 판정

계획서 §M11 게이트 표에 대입한다.

| M11 결과 조건 | 실측 | 해당 |
|---|---|---|
| 600턴에서 제국이 60% 런 이상 형성 | 15% | 아니오 |
| 제국은 서지만 즉시 해체 | 형성 15%, 존속 중앙값 124턴 | **예** |
| 600턴에서도 실효 국가 수 90% 이상 | 75.5% | 아니오 |
| 지니(국가 간) < 0.25 | **0.190** | **예** |

**판정**

1. **M13 (지구 지도) 실행.** 재기준선 목적으로 계획대로 진행한다
2. **M14 는 M13 이후 재판정.** 지금 취소도 전면 실행도 아니다. 계획서가 예고한
   "축소 실행 (조약 C안만)" 경로다
3. **M15 투자층 보류.** 지니(국가 간) 0.190 < 0.25. 게다가 §1.1 이 보인
   **단조 감소** 추세는 지도를 바꾼다고 반드시 해소되지 않는다 — 편차의 원천이
   인프라 하나이고 그 인프라에 상한이 있는 한, M13 은 초기 편차만 키우고
   수렴 자체는 그대로일 수 있다. M13 재측정에서 **600턴 지니가 100턴 지니보다
   낮은 현상이 남아 있는지**를 반드시 확인한다

**완료 기준 대조**

| §M11 완료 기준 | 상태 |
|---|---|
| 4개 지표 측정 가능 | 완료 |
| 600턴 20런 CSV 산출 | 완료 |
| 유령값 0 | 완료 (`test_law_hints.gd` PASS) |
| 게이트 판정 확정 | 완료 |
| 회귀 테스트 11종 PASS | 완료 (`tools/run_tests.sh`, §4.1) |
| 결정론 (동일 시드 바이트 일치) | 완료 (5개 CSV) |

---

## 4. M11 이 새로 드러낸 것 — 전부 처리 완료

계측 마일스톤이 "계측 도구를 못 믿는다" 로 끝나서는 안 되므로 세 건 모두 이 안에서 닫았다.

### 4.1 회귀 테스트가 실패를 실패로 보고하지 않았다

Godot 의 `assert` 실패는 스크립트 에러를 남기고 **그 함수만** 중단시킨다. `_initialize`
는 계속 진행해 마지막 줄의 `print("... PASS")` 와 `quit(0)` 에 도달한다. 즉
**종료 코드도 마지막 줄도 거짓말을 한다.**

`tools/run_tests.sh` 신설. 11종을 돌리고 출력의 `SCRIPT ERROR` 를 실패로 세어
하나라도 있으면 exit 1 한다.

```
tools/run_tests.sh              # 11 종 전부
tools/run_tests.sh test_war     # 골라서
```

#### 깨져 있던 테스트 4종의 실제 원인

전부 **테스트가 낡은 것**이었고 시뮬 결함은 없었다. (M11 변경 이전부터 깨져 있었다 —
변경 전후 실패 건수가 같음을 확인했다.)

| 원인 | 영향 | 조치 |
|---|---|---|
| 합성 세계 헬퍼가 `rng_pool` 을 안 만든다 | `SCRIPT ERROR` 218건 중 **약 205건** | 13개 헬퍼 전부에 `RngPool.new(고정시드)` 부여 |
| 봉기가 확률 게이트를 지난다 (`REBELLION_CHANCE` 0.35, M8.5) | 반란 관련 4건 | 한 틱이 아니라 **터질 때까지** 돌린다 (`_tick_until_rebellion`) |
| 1프로빈스 가드가 남기는 불만이 0.99 → `REBELLION_FUSE - 0.01` 로 바뀜 | 1건 | 상수로 대조하게 수정 |
| 공성 테스트가 인구의 **20%** 를 들이댄다 | 1건 | `SIEGE_TROOP_NORM`(인구 1% = 표준 속도)으로 맞춤 |
| 전투에 운(luck)이 들어와 고정 손실값이 바뀜 (M10.3) | 1건 | "결정론" 을 *재현성* 으로 재정의 — 두 번 돌려 대조 + 기준값 갱신 (58/81) |
| 공납이 충성도 비례로 바뀜 (`TRIBUTE_LOYALTY_SPAN`) | 1건 | 상수에서 기대값을 계산하도록 수정 |

`rng_pool` 누락이 압도적이다. **M8.5·M10.3 이 반란과 전투에 RNG 를 넣을 때 테스트
헬퍼가 따라가지 않았고, 하네스가 그것을 감춰 준 것**이 1차가 "회귀 테스트 10종 PASS"
로 결산한 실체다.

**결과: 11종 전부 PASS, exit 0.**

### 4.2 `Military.create_army` 가 군대 색인을 갱신하지 않았다

`world.armies_by_province` 는 10단계(`Military.tick`)의 `rebuild_army_index()` 에서만
채워졌다. 그래서

- 11단계(`Unrest`)에서 태어난 **반란군은 그 턴이 끝날 때까지 색인에 없다**
- `WarAI.plan` 안에서 분할·분견된 군대(`war_ai.gd:138`, `:276`)가 같은 패스의
  `armies_at()` 읽기(`:296`, `:578`)에 보이지 않는다

`create_army` 에서 살아 있는 군대를 즉시 색인에 넣도록 고쳤다.

> **밸런스 영향 0 — 20런 × 600턴 출력이 수정 전후 바이트 동일하다.**
> 우려한 AI 판단 변화는 이 기준선에서 실제로 발생하지 않았다. §3 의 게이트 판정은
> 그대로 유효하다.

`tools/test_view.gd` 의 교전 판정도 `world.armies` 를 직접 훑도록 고쳤다 — 지도
렌더러가 읽는 출처와 같게 맞춘 것이다. (이 테스트는 M11 의 시뮬 변경으로 세계
전개가 달라지면서 처음 깨졌다. 색인 결함이 원래 있었고 이번에 드러났을 뿐이다.)

### 4.3 부채 지표 — 틀린 열을 봤고, 그 뒤에 진짜 결함이 있었다

`debt_finite` 는 `mean_debt_ratio` 를 검사했는데 §6.2 의 기준은 ***최대*** 국가
부채/GDP < 10 이다. 1차가 404 를 FAIL 로 적어 놓고도 이 줄은 PASS 를 찍고 있었다.

최대값으로 바꾸자 **7174** 가 나왔다. 그런데 이 숫자는 세 가지를 한 칸에 섞고 있었다.

#### 파산은 부채를 줄인다 — 늘어난 게 아니라 분모가 사라진 것이다

```gdscript
n.debt = minf(n.debt * DEBT_HAIRCUT, n.gdp * DEBT_ANCHOR * DEFAULT_DEBT_CEILING)
```

파산 2057건의 **탕감 직후 부채/GDP 최대가 정확히 3.500** — 상한이 물린다.
7174 는 부채가 는 것이 아니다. 전형적인 경로(시드 10, 국가 34):

```
147  prov 8   gdp 79,620,170   debt 72,693,626   ratio   0.9
148  prov 5   gdp 68,362,837   debt 73,485,098   ratio   1.1
150  prov 1   gdp    251,317   debt 74,555,704   ratio 296.7   ← 반란이 7개를 떼어 갔다
153  prov 1   gdp    207,901   debt    727,654   ratio   3.5   ← 파산·탕감
155  prov 1   gdp    187,872   debt          0   ratio   0.0
```

만성 수준은 **0.9~1.2**. 최대값 하나로는 이것과 위 스파이크를 구분할 수 없다.

#### 지표를 셋으로 나눴다

| 열 | 뜻 | 실측 (20런 × 600턴) | 판정 |
|---|---|---:|---|
| `agg_debt_ratio` | 총부채 / 총GDP | 1.836 (최대 2.425) | **PASS** |
| `median_debt_ratio` | 국가별 비율의 중앙값 | 1.810 | 참고 |
| `debt_over_turns_max` | 한 나라가 10배를 **연속으로 넘긴 최장 턴수** | **42턴** | **FAIL** |
| `max_debt_ratio` | 최대값 (분모 붕괴 포함) | 7174 | 참고 |

`max_debt_ratio_solvent`(화폐 발행·파산 유예가 아닌 나라의 최대)도 만들어 봤지만
**버렸다.** `income` 역시 파이프라인 4단계에서 기록되므로 영토를 잃은 그 턴에는
아직 흑자로 보인다 — 필터가 과도 상태를 못 걸러냈다(296.7 그대로 통과).
M3 앵커에서 쓴 것과 같은 방식, 즉 **초과의 지속 시간**이 과도와 만성을 가른다.

#### 그리고 만성 결함이 실제로 있었다 — 20/20 시드

`debt_over_turns_max` 는 20런 전부 **30~42턴**(평균 37.8, 표준편차 2.2)이다.
이상치가 아니라 구조다. 시드 18 의 42턴짜리를 추적하면 기전이 나온다.

```
122  prov 3  gdp   869,777  debt 20,822,502  ratio 23.9  timer  0   ← 영토 상실
126  prov 1  gdp    89,204  debt  1,035,783  ratio 11.6  timer 25   ← 파산·탕감했는데 11.6
...                                                       25턴 동결
151  prov 1  gdp    59,984  debt  1,034,699  ratio 17.2  timer  0   ← 오히려 올랐다
163  prov 1  gdp    61,141  debt  1,034,699  ratio 16.9  print 12
164  prov 1  gdp    61,334  debt    214,669  ratio  3.5             ← 2차 파산에서야 해소
```

`trigger_default` 는 상한을 **파산 시점의 GDP 한 번**으로만 물린다. 그 시점 GDP
296K 의 3.5배인 1.03M 으로 깎았으나, 같은 턴 뒤쪽 단계(11 반란 · 13 강화)에서
영토를 더 잃어 GDP 가 89K 로 떨어졌다. **탕감 직후 이미 11.6.** 이후 25턴
(`BANKRUPTCY_TURNS`) 동안 부채는 동결이고 GDP 만 줄어 17.2 까지 오른다.
25턴 + 발행 12턴 = 37턴이 평균값의 정체다.

> **M17 §6.2("부채 상한을 파산 시점이 아니라 상시 적용")의 정확한 증거다.**
> 지시에 따라 M11 에서는 고치지 않았다. 이제 그 수정의 효과를 잴 지표
> (`debt_over_turns_max` ≤ 15)가 있다.

> **M12 추기**: 실효 국가(프로빈스 ≥ 3) 한정으로 다시 재면 이 42턴은 **3턴**으로
> 줄어든다(20런 평균 1.9턴, `TUNING_M12.md` §2). 만성 채무는 1프로빈스 잔존국에만
> 있는 현상이다 — 위 시드 18 추적도 `prov 1` 구간에서만 초과가 지속된다.
> M17 §6.2 의 우선순위는 그만큼 내려간다.

### 4.4 `Economy.aggregate` 가 영토 변동을 한 턴 늦게 반영했다

`aggregate` 는 파이프라인 4~6단계에서만 돌았다. 그런데 영토가 움직이는 곳은
**11단계(반란)와 13단계(강화조약)** 다. 나라를 반쯤 잃은 국가가 그 턴 내내
잃기 전의 `gdp` · `population` · `avg_unrest` 를 들고 있었고, 12~14단계
(`Diplomacy` · `Peace` · `EmpireSystem` · `Market`)가 전부 그 낡은 값으로
위협·강화·행정·시장을 판단했다.

```gdscript
Unrest.tick(world)                 # 11. 불만 누적, 반란 발생
Economy.aggregate(world)           # 11.5 반란이 떼어 간 땅을 집계에 반영
Diplomacy.tick(world)              # 12. 관계 갱신, 선전포고
Peace.tick(world)                  # 13. 전쟁 점수·강화 판정
Economy.aggregate(world)           # 13.5 강화조약이 옮긴 땅을 집계에 반영
EmpireSystem.tick(world)           # 13.6 통합·행정·권위·속국 충성
```

**밸런스 영향 — 0 이 아니다.** 600턴 20런 기준선 대비:

| 지표 (600턴) | 수정 전 | 수정 후 | 차이 |
|---|---:|---:|---:|
| 전체 국가 수 | 72.25 | 69.80 | −3.4% |
| 실효 국가 수 | 29.00 | 29.50 | +1.7% |
| 최대국 영토 | 22.30 | 21.25 | −4.7% |
| 최대 realm 점유 | 0.1028 | 0.0973 | −5.3% |
| 지니 (국가 간) | 0.1851 | 0.1895 | +2.4% |
| 제국 형성 런 | 5 / 20 | 3 / 20 | 노이즈 범위 |

세계가 조금 덜 조각나고(반란 신생국 1951 → 1907) 최대국이 조금 작아진다.
제국 형성 런의 감소는 n=20 에서 판정 불가다 (§2.3 경고). **게이트 판정은 불변.**

`n.income` 은 여전히 4단계 값이다 — 그 턴에 실제로 그 땅에서 걷은 세수이므로
낡은 것이 아니라 맞는 값이다. 고치지 않았다.

### 4.5 인프라 포화 — M17 으로 넘긴다

600턴 20런 전부 최고 인프라 **10.00 = `INFRA_MAX`**. §6.6 이 대역을 9~11 로 옮기라고
했지만, 하드캡에 붙어 버리면 그 지표는 상수를 재는 것이 된다. §1.1 의 지니 붕괴와
같은 뿌리다. M17 §6.6 을 "대역 이동" 이 아니라 **"인프라가 편차의 원천으로
계속 작동하는가"** 로 다시 써야 한다.

## 5. 코드 변경 목록

| 파일 | 변경 |
|---|---|
| `sim/systems/economy.gd` | `province_anchor()` 추출, `trade_mult()` 신설 (`TRADE_W` 0.3 · `TRADE_INLAND_SHARE` 0.3), 생산 틱이 앵커를 기록 |
| `sim/systems/unrest.gd` | `LAW_UNREST_W` 0.15, `drift` 에 법률 `unrest` 항 추가 |
| `sim/model/province.gd` | `anchor_gdp_pc` 필드 (계측용, 생산 틱이 기록) |
| `tools/batch_sim.gd` | 앵커 불변식 판정 재작성, 지니 2종·전쟁 보급·실효 국가·realm 점유·제국 수·부채 3종 열 추가, `_nations.csv` · `_empires.csv` 산출 |
| `tools/analyze.py` | M3 판정 수정, 부채 판정을 집계·지속시간으로 분리, M11.1 4종 · M11.2 스냅샷 · 제국 생애사 · 게이트 판정 절 추가 |
| `tools/test_law_hints.gd` | **신설** — 힌트 ↔ 소비처 전수 대조 회귀 테스트 |
| `tools/test_view.gd` | 교전 판정을 `world.armies` 기준으로 (렌더러와 같은 출처) |
| `tools/run_tests.sh` | **신설** — 11종 실행, `SCRIPT ERROR` 하나라도 있으면 exit 1 |
| `sim/systems/military.gd` | `create_army` 가 군대 색인을 즉시 갱신 (밸런스 영향 0, 바이트 동일 확인) |
| `sim/sim_clock.gd` | 11.5 · 13.5 단계에 `Economy.aggregate` 추가 (영토 변동 즉시 반영, §4.4) |
| `tools/test_{war,military,rebellion,empire,credit,market,naval,characters}.gd` | 합성 세계에 `rng_pool` 부여, 확률 게이트·상수 변경에 맞춰 단언 갱신 |
