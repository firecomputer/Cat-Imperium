# M12 — 지표 재정의 ((a)의 실행)

> 대상: 2차 §0.1 · §M12. 시뮬 동작은 **한 줄도 바꾸지 않았다** — 계측·분석만 바꿨다.
> 산출물: `out/m12_600.csv` (+ `_defaults` `_credit_events` `_nations` `_empires`),
> 20런 × 600턴, 시드 1~20.

## 0. 결정론 회귀

M11 기준선(`out/m11_600b.csv`)과 **기존 39개 열 전부 바이트 동일**, 사이드카 4종
(`_defaults` 2057행 · `_nations` 2706행 · `_empires` 3행 · `_credit_events` 14337행)도
전부 동일. 신설 10개 열만 늘었다. 테스트 11종 PASS.

---

## 1. 무엇을 바꿨나

| 항목 | 구 | 신 | 근거 |
|---|---|---|---|
| 첫 파산 대역 | 100~250 | **80~250** | §0.1 — 제국 형성을 위한 개전 압력 확대 |
| 파산 지표 모집단 | 전 국가 | **실효 국가(프로빈스 ≥ 3)** | §0.1 |
| 국가 수 지표 | 전 국가 | **실효 국가** | §0.1 |
| 제국 유지 턴 | 12 | **24** | §M12 — 100턴 만에 풀리는 것을 형성으로 세지 않는다 |
| 제국 해체율 | 없음 | **신설, 40~70%** | §M12 — 붕괴 나선의 진짜 검증 지표 |

### 1.1 이중 산출 — 판정은 실효, 전체는 괄호

`_band2()` 가 `실효 x [대역] PASS/FAIL (전체 y)` 한 줄로 찍는다. 지니·보급·부채·
파산 생존율·수명·국가 수 전부 이 형식이다. 신설 열 10개:

```
gini_between_eff  gini_within_eff  war_supply_mean_eff  war_armies_eff
mean_debt_ratio_eff  max_debt_ratio_eff  agg_debt_ratio_eff
median_debt_ratio_eff  debt_over_turns_max_eff  empires_active_24
```

### 1.2 제국 24턴은 재실행 없이 재도출된다

`_empires.csv` 의 `duration` 은 **후보 진입부터 조건 이탈까지의 연속 턴 수**와 같다
(`enter_turn` 이 후보 시작 턴이므로). 따라서 `duration >= 24` 필터는 `confirm=24` 로
다시 돌린 것과 **수학적으로 동일**하다. 그래서 배치의 에피소드 로그는 느슨한 쪽
(12턴)으로 남긴다 — 24로 기록하면 1차 정의와의 대조가 영영 불가능해진다.
스냅샷의 `empires_active_24` 만 24턴을 쓴다.

---

## 2. 신 지표 세트 PASS/FAIL 표 (20런 × 600턴)

| 지표 | 실효 | 전체 | 대역 | 판정 |
|---|---:|---:|---|---|
| 첫 파산 턴 | **134.8** (중앙값 127.5, 범위 70~270) | 97.4 | 80~250 | **PASS** |
| 집계 부채/GDP | **1.912** (최대 2.464) | 2.425 | < 10 | **PASS** |
| 10배 초과 최장 지속 | **3턴** (평균 1.9) | 42턴 | ≤ 15 | **PASS** |
| 전쟁 중 평균 보급률 | **0.7955** | 0.7902 | 0.60~0.80 | **PASS** |
| 월경지 수명 < 본토 수명 | 583.0 vs 589.1 | 583.1 vs 589.4 | 월경지가 짧을 것 | **PASS** |
| 제국 해체율 (관측 창) | **0.667** (절단 제외 시 1.000) | — | 0.40~0.70 | **PASS** |
| 실효 국가 잔존률 | **75.5%** (39.1 → 29.5) | 174.7% | 40~70% | **FAIL** |
| 제국 형성률 (유지 24턴) | **15%** (3/20런) | — | ≥ 60% | **FAIL** |
| 1인당 GDP 지니 (국가 간) | **0.1260** | 0.1895 | 0.30~0.50 | **FAIL** |
| 1인당 GDP 지니 (국가 내) | **0.0854** | 0.0844 | 0.20~0.40 | **FAIL** |
| 파산 후 50턴 생존율 | **0.9875** (n=80) | 0.9875 (n=1845) | 0.40~0.60 | **FAIL** |
| 파산 후 100턴 생존율 | **0.9730** (n=74) | 0.9615 (n=1611) | 0.40~0.60 | **FAIL** |
| 최고 인프라 | 10.00 (상한 포화) | — | 7~9 | **FAIL** (M17 §6.6) |

**6 PASS / 7 FAIL.** M11 표 대비 뒤집힌 것은 아래 두 줄이다.

### 2.1 첫 파산: FAIL → PASS, 그런데 대역 이동 때문이 아니다

- 전체 기준 97.4턴 — 구 대역(100~250)에서 FAIL, 신 대역(80~250)에서는 이것도 PASS.
- 실효 기준 134.8턴 — 대역 한가운데다.

즉 §0.1 이 예상한 "대역을 넓혀야 통과한다" 가 아니라 **모집단을 고치니 애초에
대역 중앙이었다**. 파산 2057건 중 프로빈스 1개인 것이 1872건(91%)이다.
전체 기준의 97.4턴은 세계가 멀쩡할 때 먼저 무너지는 잔존국을 재고 있었다.

### 2.2 만성 부채: FAIL → PASS. M11 §4.3 의 결함은 잔존국 한정이었다

| | 전체 | 실효 |
|---|---:|---:|
| `debt_over_turns_max` 시드별 평균 | 37.8턴 | **1.9턴** |
| 최소~최대 | 30~42 | **0~3** |
| 최대 부채/GDP | 7174 | 47.7 |

20런 전부 3턴 이하다. M11 §4.3 이 시드 18 에서 추적한 42턴짜리도 `prov 1` 구간에서만
초과가 지속됐다 — `trigger_default` 의 상한이 파산 시점 GDP 한 번으로만 물린다는
기전 자체는 그대로지만, **실효 국가에서는 그 경로가 3턴 안에 닫힌다.**

> **M17 §6.2(상시 부채 상한)의 우선순위를 내린다.** 결함은 실재하나 영향 범위가
> 1~2 프로빈스 잔존국이고, 그 잔존국은 신 지표 모집단에서 빠진다.

### 2.3 지니가 실효 기준에서 *더 낮다* — M15 보류가 굳어진다

| | 전체 | 실효 |
|---|---:|---:|
| 국가 간 | 0.1895 | **0.1260** |
| 국가 내 | 0.0844 | **0.0854** |

직관과 반대지만 기전은 단순하다. **편차를 만들던 것이 잔존국이었다.** 프로빈스
1~2개짜리 나라들이 분포의 양 끝을 늘리고 있었고, 그것을 빼면 실효 국가 29.5개는
서로 거의 같은 1인당 GDP 를 가진다. 스냅샷이 이를 그대로 보여준다.

```
turn   실효국가  전체국가   지니간   지니간실효
0        39.1    40.0     0.219    0.218
200      32.0    47.4     0.257    0.215
400      30.4    60.9     0.186    0.117
600      29.5    69.8     0.190    0.126
```

400턴 이후 두 값이 갈라지는 지점이 반란 신생국(600턴에 57.6개)이 누적되는 지점이다.

> M15 착수 조건 ≥ 0.25 대비 **0.126.** M11 의 0.190 보다 더 멀다.
> **투자층 보류 유지.** M13 지도 교체 뒤 재판정.

### 2.4 실효 국가 잔존률이 지표를 처음으로 말이 되게 만들었다

전체 기준 잔존률은 **174.7%** 다 (40 → 69.8). 반란 독립국이 계속 태어나므로
"국가 수가 줄어드는가" 를 전체 기준으로 묻는 것 자체가 무의미했다. 실효 기준
75.5%(39.1 → 29.5)는 대역 40~70% 를 살짝 넘는 FAIL 이고, 이것이 §6.1
"국가가 줄지 않는다" 의 정직한 값이다.

### 2.5 제국: 형성 15% FAIL, 그러나 선 것은 제대로 섰다

에피소드 3건 전부 `duration ≥ 24` 라 12턴 정의와 24턴 정의의 형성률이 **같다**
(15%). 유지 턴 상향은 이 기준선에서는 아무것도 걸러내지 않았다.

```
seed nation enter exit duration peak_share  reason
10     24    350   397    47      0.1307    realm_share_below_threshold
12     28    365   489   124      0.1299    realm_share_below_threshold
13     15    442   600   158      0.1635    horizon
```

- 해체율(관측 창 기준) 2/3 = **0.667 PASS**. 우측절단 1건을 제외하면 2/2 = 1.000.
- 전부 350턴 이후에 선다. **300턴 표준 런에서는 관측조차 불가능했다** — M11.2 가
  600턴을 재라고 한 이유가 여기서 확인된다.
- `peak_realm_share` 최대 0.164. 임계 0.12 를 겨우 넘긴 채 버티다 풀린다.

---

## 3. 게이트 재판정

| M11 게이트 분기 | 실측 (M12 지표) | 결론 |
|---|---|---|
| 제국 60% 이상 형성 | 15% | 해당 없음 |
| 제국은 서지만 즉시 해체 | 3런에서 형성, 존속 중앙값 124턴, 해체율 0.667 | **해당** |
| 실효 국가 90% 이상 잔존 | 75.5% | 해당 없음 |
| 지니(국가 간) < 0.25 | 0.126 | **해당** |

> **M13 실행 → M14 축소(조약 C안) 재판정 → M15 보류.**
> M11 판정과 방향은 같고, 근거 수치가 실효 기준으로 교체됐다.
> 바뀐 것은 M17 §6.2 의 우선순위 하향 하나다.

---

## 4. 코드 변경 목록

| 파일 | 변경 |
|---|---|
| `tools/batch_sim.gd` | `FIRST_DEFAULT_MIN` 100→80. `EMPIRE_CONFIRM_TURNS_M12 := 24` 신설. `_track_empires` 가 24턴 기준 활성 제국 수를 반환. `_gini_between`/`_gini_within`/`_war_supply` 에 `eff_only` 인자. `_sample_row` 에 실효 부채 집계 + 신설 10개 열 |
| `tools/analyze.py` | `FIRST_DEFAULT_RANGE` (100,250)→(80,250). `EFFECTIVE_MIN_PROVINCES = 3`. `_band2()`(이중 산출) · `_col()`(구 CSV 호환) 신설. M5·부채·지니·보급·파산 생존율·수명·국가 수 전부 이중 산출. 제국 판정을 24턴 정의로 이동, 해체율에 우측절단 제외값 병기 |
| `TUNING_M11.md` | §4.3 에 M12 추기 (만성 부채는 잔존국 한정) |
