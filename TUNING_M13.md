# M13 착수 — 실지도 생성과 런타임 전환

> **현재 범위**: `CAT_IMPERIUM_PHASE2.md` §M13.1~M13.5.1의 실제 지도 생성·구운 에셋·
> 런타임 기본값 전환과 §M13.7의 지도/프로빈스 불변식 검증.
> **상태**: 실지도 생성 완료(440×200, 국경 기반 프로빈스). 문화 12종·M13.8 재기준선·
> M13.9 게이트는 아직 남았다.

## 1. 생성 파이프라인

`tools/build_earth_map.py`가 로컬 원본을 읽어 `data/world/earth_map.bin`을 굽는다.
런타임은 셰이프파일이나 GeoTIFF를 읽지 않는다.

```bash
.venv/bin/python tools/build_earth_map.py
godot --headless --script res://tools/dump_map_png.gd -- \
  --map-source earth --out res://out/earth.png
```

| 입력 | 용도 |
|---|---|
| `ne_10m_land` | 헥스별 육지 교차면적 |
| `ne_10m_lakes` | 호수 피복 차감·내해 보존 |
| ETOPO 2022 60s bed | 헥스 중심+꼭짓점 표고 평균 |
| `ne_10m_admin_0_countries` | 타일별 지배 `ADMIN` 판정 — 프로빈스 경계의 근거 |

- 투영: Behrmann 원통 등적, 표준위도 30°
- 격자: **440×200**, odd-r, 총 88,000타일
- 위도: `LAT_MIN=-60`, `LAT_MAX=90`
- 점수: `육지 피복률 - 호수 피복률 + 정규화 표고 × 0.015`
- 순위 선택: 상위 **25,000타일**. 임계값은 쓰지 않는다
- 국가: 헥스와 교차 면적이 가장 큰 `ADMIN`. 어느 국가 도형과도 안 겹치는 육지
  타일은 육지 이웃의 최빈 국가를 전파받는다. 무주지 타일은 남지 않는다
- 원본 URL·로컬 파일 SHA-256은 생성 스크립트와 manifest에 남긴다

### 1.1 격자를 150×100에서 440×200으로 올린 이유

150×100은 x/y 스케일이 독립이라 헥스가 동서로 늘어나 있었다.

| 축 | 격자 단위 | km/단위 (150×100) | km/단위 (440×200) |
|---|---:|---:|---:|
| x = 2πR·cos30° = 34,713 km | W+0.5 | **231** | **78.8** |
| y = R(sin90−sin(−60))/cos30° = 13,725 km | (H−1)·√3/2 + 2/√3 | **158** | **79.1** |
| 비등방 | | 1.46 | **1.00** |

대한해협(부산~규슈)은 약 200 km라 231 km 셀에서는 물 타일이 한 칸도 들어가지
않았다 — 한반도와 일본이 한 덩어리였다. 등방 조건
`(W+0.5) / ((H−1)·√3/2 + 2/√3) = 2.528`을 풀면 H=200에서 W=438.6이고, 440으로
잡으면 오차 0.3%다. 79 km 셀에서 해협은 2.5칸이 되어 갈라진다.

`LAND_TARGET`은 25,000. 기존 5,000/15,000 = 33%는 실제 육지 비율(남극 제외
135M km² / 476M km² = 28.4%)보다 과대했다. 25,000 × 5,378 km² = 134M km²로
실측과 맞는다.

최종 생성물을 같은 입력으로 두 번 구웠을 때 두 파일 모두 바이트 동일했다.

```text
earth_map.bin   4a494847393a59df160d4f91ae2f16e989712cb996fb54e74723dd14cd7c0ef9
earth_map.json  c00cc0f82d2fe70378505f82b561780dbdb9221e674222b733b778cc21181c1f
```

## 2. 지도 불변식

| 항목 | 결과 | 판정 |
|---|---:|---|
| 육지 | **25,000** | PASS |
| 연결 성분 | **75** | PASS (≥12) |
| 최대 성분 | **14,821 / 59.3%** | PASS (≤70%) |
| 2위 성분 | **7,735** | PASS (≥4,500) |
| 명명 소도서 | **31/31 = 100%** | PASS (≥90%) |
| 주요 요충지 | **6/6** | PASS |
| 국가 | **257개**, 국경으로 갈린 육지 그룹 **248개** | — |

보존 섬은 NE 육지 도형과 실제로 교차하는 후보 타일 중 각 명명 좌표에 가장 가까운
타일을 강제 승격한다. 승격한 수만큼 최하위 비강제 육지를 차감해 25,000을 유지한다.

79 km 해상도에서 한반도(성분 5, 아프로유라시아)와 일본(성분 18)이 갈라졌고
대만·홋카이도도 독립 성분이 됐다. 반대로 간몬·세토 내해는 해상도 아래라
혼슈·규슈·시코쿠는 한 덩어리로 남는다.

요충지는 일반 위상 태거 위에 구운 권위 태그를 얹었다.

- `ISTHMUS`: 수에즈, 파나마
- `STRAIT`: 지브롤터, 보스포러스, 말라카, 베링

지브롤터·보스포러스는 양안이 같은 아프로유라시아 연결 성분이라 기존의
"서로 다른 대륙 두 개에 인접" 규칙만으로는 해협이 될 수 없다. 실제 지구의 명명
요충지는 데이터 태그가 위상 휴리스틱보다 우선해야 한다.

## 3. 프로빈스 경계 = 실제 국경

프로빈스는 **국경을 넘지 않는다.** 굽는 단계에서 타일마다 지배 `ADMIN`을 정하고,
런타임 분할기는 육지 연결 성분을 다시 국가 단위로 쪼갠 그룹 위에서만 성장한다.
성장·고아 흡수·큰 덩어리 재분할이 전부 이 그룹 배열 하나만 보므로 그룹 정의를
바꾸는 것으로 충분하다.

여기서 한 가지 함정이 있었다. `_smallest_adjacent`가 흡수 대상을 고를 때 그룹을
보지 않았다. 그룹이 지리 성분이던 시절에는 서로 다른 성분이 헥스 인접일 수 없어
드러나지 않던 버그다. 국경으로 그룹이 갈리자 고아 클러스터 56개가 이웃 나라
프로빈스로 흡수됐다. 흡수 후보를 같은 그룹으로 제한해 **0**이 됐다.

> 국경은 프로빈스 **모양**에만 쓴다. 어느 프로빈스를 어느 나라가 갖는지는 여전히
> 시드가 정한다 (§M13.5).

지구 지도 40시드에서 프로빈스 분할을 재실행했다.

| 항목 | 결과 |
|---|---:|
| 프로빈스 수 | 평균 **912.1** |
| 평균 크기 | **27.4타일** |
| 1~52타일 위반 | **0/40런** |
| 고아/중복 타일 | **0/40런** |
| 섬의 나라별 조각이 독립 프로빈스 아님 | **0/40런** |
| 국경을 넘는 프로빈스 | **0** |
| 국가 수 | **40 × 40런 = 1,600** |

목표는 850이었고 912가 나왔다. 국경이 강제하는 하한(248 그룹)과 작은 나라들이
한 프로빈스씩 차지하는 몫이 얹힌 결과다.

| 나라 | 타일 | 프로빈스 |
|---|---:|---:|
| Russia | 3,157 | 106 |
| China | 1,751 | 56 |
| France | 119 | 6 |
| Japan | 65 | 2 |
| Germany | 66 | 2 |
| South Korea | 20 | 1 |
| Switzerland | 7 | 1 |

타일에 9개 광역 지역 라벨과 위경도를 굽고, 프로빈스는 다수 타일 지역을, 국가는 건국
수도의 지역을 보존한다. `batch_sim`의 `_nations`·`_empires` 사이드카가
`start_region`을 출력한다. 40런의 시작 지역 분포는 **40가지 전부 상이**해서 고정된
것은 지리뿐이고 정치 배치는 시드가 바꾼다는 규약을 만족했다.

## 3.1 granularity — 노이즈 기준선을 깨지 않고 규격을 키우는 법

프로빈스·해역 분할 상수는 노이즈와 지구가 공유한다. 상수를 그냥 키웠더니
`test_naval`이 노이즈에서 깨졌다(해역 110 → 58). 그래서 배율 하나를 지도 소스가
싣고 다니게 했다: `NoiseMapSource` 1.0, `EarthMapSource` 1.74.

| 상수 | 기준(1.0) | earth(1.74) | 배율식 |
|---|---:|---:|---|
| `ProvinceSplitter.MAX_TILES` | 30 | 52 | × g |
| `TARGET_MIN` / `TARGET_MAX` | 8 / 30 | 14 / 52 | × g |
| `AVG_TARGET` | 18.5 | 32.2 | × g |
| `SEED_MIN_DIST` | 3 | 4 | × √g |
| `CHUNK_SIZE` | 22 | 38 | × g |
| `FeatureTagger.ISLAND_MAX_TILES` | 30 | 52 | × g |
| `SeaSplitter.AVG_TARGET` | 90 | 157 | × g |
| `SeaSplitter.TARGET_MIN` / `MAX` | 60 / 130 | 104 / 226 | × g |
| `SeaSplitter.SEED_MIN_DIST` | 6 | 8 | × √g |

g=1.0에서 모든 값이 M12와 정확히 같아 노이즈 기준선이 그대로 산다.

`NationPlacer.CAPITAL_MIN_DIST`는 격자 평면 거리라 배율이 다르다. 세계가 커진 만큼
같은 "세계 크기 대비 간격"이 되도록 √(육지 타일 수 / 5000)을 곱한다 —
노이즈 9.0, 지구 20.1.

## 4. MapSource 방어선

```text
MapSource
├─ EarthMapSource  440×200, granularity 1.74, 구운 고정 지리·국경, 제품 기본값
└─ NoiseMapSource  100×150, granularity 1.00, M1~M12 절차 생성기 어댑터, 회귀 기준선
```

격자 폭·높이를 전역 상수로 읽던 프로빈스·해역·렌더러를 월드 차원으로 바꿨다.
`--map-source earth|noise`로 지도·프로빈스·배치 도구를 선택할 수 있다.

NoiseMapSource에서 시드 1, 600턴을 다시 실행해 M12 기준선의 메인 CSV 62행과
**바이트 동일**을 확인했다. 격자 확대·국경 도입·`granularity` 도입·
`_smallest_adjacent` 수정을 전부 얹은 뒤에도 같은 해시다.

```text
f08554fec6aeae868a25dfc51a3e027ffb7202b4064286ad67d3e4c70545ab1b
```

EarthMapSource도 같은 시드 50턴을 두 번 실행해 메인 CSV와 사이드카 4종이 모두
바이트 동일했다.

### 4.1 에셋 포맷

`FORMAT_VERSION`을 2로 올렸다. 타일당 12바이트다.

| 필드 | 타입 | 비고 |
|---|---|---|
| land | u8 | |
| elevation | u16 | m, 런타임에서 6000으로 정규화 |
| land_coverage | u8 | 피복률 × 255 |
| longitude / latitude | u16 × 2 | ×100 오프셋 인코딩 |
| regions | u8 | 9개 광역 지역 |
| forced_features | u8 | 구운 요충지 태그 |
| **country** | **u16** | **v2 신규. 0 = 없음, 1.. = manifest `countries` 색인** |

`earth_map.bin` 1,056,016 바이트 (150 KB → 880 KB → 1.06 MB).

### 4.2 성능

`performance` 프로필 단독 실행 기준 (Ryzen AI 9 HX 370, 5.16 GHz).

| 지도 | 프로빈스 | 해역 | 타일 | 600턴 1런 |
|---|---:|---:|---:|---:|
| earth 440×200 | 912 | 453 | 88,000 | **77.3초** |
| noise 100×150 | 310 | 118 | 15,000 | **20.9초** |

3.7배. §M13.8의 40런 × 600턴은 약 52분이다.

> 처음 잰 163초는 `platform_profile=quiet` + `powersave`로 CPU가 2.0 GHz에 묶인
> 상태에서 다른 두 작업과 동시에 돌린 값이라 폐기했다.

## 5. 테스트

기존 노이즈 회귀 11종은 그대로 고정하고 `test_earth_map.gd`를 추가했다.

```text
tools/run_tests.sh  → 12종 전부 PASS
제품 기본 Earth 헤드리스 실행 → SCRIPT ERROR 0
```

`test_earth_map.gd`의 대역도 새 격자에 맞춰 갱신했다 — 440×200, 육지 25,000,
프로빈스 750~950, 상한은 `ProvinceSplitter.max_tiles(EarthMapSource.GRANULARITY)`.

## 6. 아직 닫지 않은 M13

- 저장소에 `ne_10m_rivers_lake_centerlines`가 없어 `has_river` 태깅은 아직 없다
- 별도 `ne_10m_minor_islands`가 없어 이번 단계는 NE land 교차 + 31개 명명 앵커로 대체했다
- §M13.6 문화 앵커 다이얼과 §M13.7-a 문화 12종/`assimilation`은 미착수
- M13 착수 전 신설 계측(실효국가 이탈률, 인프라 표준편차·포화율 등)은 미착수
- §M13.8 40런×600턴, 800턴 프로브와 §M13.9 지니 시간 미분 게이트는 미실행

### 6.1 격자 확대가 미룬 숙제 — 수도 거리 재보정

`distance_from_capital`은 두 경로로 나온다.

- 본토: 프로빈스 그래프 BFS 홉 수 (`nation_placer.gd:177`) — 프로빈스가 3.1배가
  되어 약 **1.7배** 증가
- 월경지: centroid 직선거리 × 0.5 (`nation_placer.gd:180`) — 격자 평면 거리가
  **2.42배** 증가

이 값이 `unrest.gd:128`(`DISTANCE_CAP`), `economy.gd:68`(건설비 ×0.015/단위,
상한 없음), `empire_system.gd:169`(상한 10.0), `war_ai.gd:190`으로 들어간다.
특히 `EXCLAVE_DIST_CAP := 12.0`은 그대로 두면 거의 모든 월경지가 상한에 포화되어
변별력을 잃는다.

밸런스 변경이라 지도 작업과 분리했다. **§M13.8 재기준선에서 `EXCLAVE_DIST_CAP`,
× 0.5 계수, `economy.gd`의 0.015를 함께 잡아야 한다.**

따라서 이 문서는 **실지도 생성 완료 기록**이지 M13 전체 완료 판정이 아니다.

---

# M13 2차 — 문화 12종·국가 수 규격·재기준선

1차 기록이 남긴 세 가지(문화 12종, §M13.8 재기준선, §M13.9 게이트)를 여기서 닫는다.
회귀는 `test_culture`를 더해 **13종**이 되었고 전부 PASS, 노이즈 지표는 불변이다.

## 7. 지구 지도에서 제국이 서지 않던 진짜 이유

`TUNING_M14.md` §2.4는 "지구 40런에서 제국 형성 0/12"를 기록하고 원인을 조약 문법
쪽에서 찾았다. 실측해 보면 원인은 지도도 조약도 아니고 **`NationPlacer.NATION_COUNT = 40`
이라는 상수 하나**였다.

| turn 0 | noise | earth (40국 고정) |
|---|---:|---:|
| 프로빈스 / 국가당 | 310 / 7.8 | 917 / **22.9** |
| `admin_load` / `admin_capacity` | 9.1 / 13.1 | 28.8 / 19.1 |
| load/cap | 0.69 | **1.51** |
| 과확장 국가 | 4/40 | **37/40** |

`EmpireSystem`의 `capacity` 상수항(`6.0 + political×4.0`)은 국가당 8칸 세계에 맞춘
값이고 `load`는 프로빈스당 약 1.25로 선형 증가한다. 지도가 3배가 되면 국가는
**건국 시점부터 과확장**이다. 그 뒤는 자동이다.

```
overextension 0.5 → _tick_authority 매 턴 −0.003 → WAR_MIN_TURN(55) 전에 권위 0.31
   → WAR_AUTHORITY_FLOOR(0.25) 개전 차단 → 정복 불가
        ↑                                              │
        └── 정복해도 load만 늘어 권위가 더 깎인다 ────────┘
```

권위 추이와 개전 게이트 (시드 1, 400턴):

| | t=50 | t=100 | t=400 | `blocked_authority` 비율 |
|---|---:|---:|---:|---:|
| noise | 0.485 | 0.469 | 0.343 | 10.9% |
| earth (40국) | **0.308** | **0.252** | 0.313 | **31.9%** |
| earth (115국) | 0.491 | 0.471 | 0.385 | 6.1% |

## 8. 규격 변경 두 가지

### 8.1 국가 수는 세계 크기가 정한다

```gdscript
NationPlacer.nation_count(land_tiles, granularity)
    = 40 × (land_tiles / 5000) ÷ granularity        # noise 40, earth 115
```

프로빈스 평균 크기가 `granularity`에 비례하므로 **국가당 프로빈스 수가 보존된다**
(earth 7.9칸, noise 7.5칸). 프로빈스 수가 아니라 육지 타일과 배율로만 정하는 이유는
프로빈스 수가 시드마다 흔들리기 때문이다 — 같은 지도 소스면 시드와 무관하게 같은
값이라야 회귀 기준선이 산다.

수도 최소 간격도 같은 규격으로 옮겼다.

```gdscript
min_dist = CAPITAL_MIN_DIST × √( (land_tiles / nation_count) / (5000 / 40) )
```

노이즈에서 정확히 9.0이 나와 1차 기준선이 보존된다. 국가 수만 늘리고 간격 공식을
그대로 뒀다면 earth의 20.1이 115개 수도를 수용하지 못해 배치가 조용히 실패한다.

### 8.2 제국 문턱도 국가 수의 함수다

`EMPIRE_THRESHOLD = 0.12`는 "세계 GDP의 12%"가 아니라 **"평균국의 4.8배"**라는
뜻이었다(40국 세계). 국가 수가 지도를 따라가면 이 절대 상수는 큰 지도에서 저절로
가혹해진다 — 115국 세계에서는 평균국의 13.8배다.

```gdscript
EmpireSystem.empire_threshold(world) = 0.12 × 40 / world.initial_nation_count
```

`batch_sim.gd`와 `batch_war.gd`가 각자 들고 있던 상수를 이 함수로 합쳤다.

### 8.3 A/B (earth, 같은 시드 3런 × 600턴)

| | 40국 | 115국 |
|---|---:|---:|
| load / capacity | 1.51 | **0.71** |
| 강화(평화조약) | 29.7/런 | **147.3/런** |
| 속국화 | 4.7/런 | **29.7/런** |
| **제국 에피소드** | **0건 (0/3런)** | **20건 (3/3런)** |
| 최장 제국 지속 | — | 344턴 |

시드 1의 최대 realm_share는 700턴에서 `t100 0.024 → t400 0.044 → t500 0.069`로
단조 상승한다. §M11이 찾던 "강해질수록 강해지는 루프"가 지구 지도에서 처음 관측된다.

> **잔여**: `first_default` 배치 게이트가 mean 84.3(PASS) → 78.3(FAIL, 하한 80)으로
> 밀렸다. "세계 최초 파산 턴"은 국가 수에 대한 최소값 통계라 표본이 40 → 115로 늘면
> 그 자체로 앞당겨진다. 40런 실측 mean 78.3, 중앙값 77, 최소 70, **50턴 미만 0건**.
> `FIRST_DEFAULT_MIN`을 국가 수로 정규화하거나 하한을 낮추고 `under_50`을 실질
> 기준으로 삼아야 한다.

## 9. 문화 5종 → 12종 (§M13.7-a)

### 9.1 신설 파라미터 `assimilation` — 이득 둘, 비용 하나

| 경로 | 방향 | 상수 |
|---|---|---|
| 이문화 프로빈스 불만 압력 | 감소 (최대 −60%) | `Unrest.ASSIMILATION_RELIEF = 0.60` |
| 이문화 프로빈스 행정 부하 | 감소 (최대 −40%) | `EmpireSystem.ASSIMILATION_ADMIN_RELIEF = 0.40` |
| **이문화 프로빈스 유지비** | **증가 (최대 +50%)** | `Economy.ASSIMILATION_UPKEEP_W = 0.50` |

동화형 문화는 정복지를 빨리 녹이는 대신 그 땅의 유지비를 매 턴 더 낸다(§5.5).
`test_culture.gd`가 세 경로를 각각 부호로 검증한다 — 유령값이 되면 회귀가 깨진다.

### 9.2 파라미터가 뭉치지 않는가

9차원 상호 거리의 최솟값은 **0.222**이고 그 최근접 쌍은 신설 문화가 아니라
**기존 샴–코리안숏헤어**다. 신설 7종은 기존 5종이 만든 최소 간격을 좁히지 않았다.
이 값을 회귀 하한(0.22)으로 못박았다.

### 9.3 티어와 배치

`TIER_SIZES = [4, 5, 3]`, `TIER_WEIGHT = [7, 3, 1]`, `RARE_ABSENT_CHANCE = 0.35`.
티어 배정은 매 판 시드로 섞고 정원은 최대잔여법으로 국가 수에 정확히 맞춘다
(40국이든 115국이든 합이 어긋나지 않는다).

문화 배치는 `CULTURE_ANCHOR_STRENGTH = 0.45`. 이번 판 정원을 지리 원산에 맞춰
나눠 주고, 앵커에 걸리지 않은 수도는 남은 정원에 비례해 뽑는다. 전용 RNG 스트림
(`culture_placement`)을 쓴다 — 수도 선정·문화 파라미터 스트림을 밀면 회귀 기준선이
통째로 어긋난다(§15). 노이즈 프로빈스는 `region = -1`이라 앵커가 자동으로 꺼진다.

실측(earth 6런 × 400턴, 판당 115국): **앵커 적중 36%**, 문화별 실효 생존율
**57~74%**(기준 각 10% 이상), 시작 국가 수 34~95로 티어 차등이 실제로 벌어진다.
미등장은 판마다 다른 문화에서 난다(이 표본은 러시안블루 2/6, 브리티시숏헤어·
이집션마우 각 1/6).

`view/map_renderer.gd`의 문화 색상표도 12종으로 늘렸다. 기존 5종의 hue는 그대로 두고
신설 7종만 빈 구간에 넣었다.

## 10. §M13.8 재기준선 / §M13.9 게이트

`tools/batch_parallel.sh --runs 40 --turns 600 --jobs 12 --map-source earth`

| 지표 | t=0 | t=100 | t=300 | t=600 |
|---|---:|---:|---:|---:|
| 실효 국가 (프로빈스 ≥ 3) | 113.5 | 105.4 | 76.0 | **68.2** |
| 최대 realm_share | 0.025 | 0.033 | 0.048 | 0.065 |
| 활성 제국 (24턴 유지) | 0.00 | 0.00 | 0.53 | **1.93** |
| 지니(국가 간, 실효) | 0.207 | **0.231** | 0.196 | 0.199 |
| 지니(국가 내, 실효) | 0.312 | **0.413** | 0.288 | 0.228 |

- **제국 형성 39/40런** (24턴 이상 에피소드 153건 / 전체 180건) → §1 판정표의
  "600턴 60% 런 이상"을 크게 넘는다. **M14는 재판정 대상이고 취소가 유력하다**
- 실효 국가 113.5 → 68.2 (60%로 감소) — "600턴에도 90% 이상" 구조 경보에 안 걸린다
- 시작 지역별 제국(24턴 이상 153건): 북아프리카·서아시아 33, 북미 26, 남미 23,
  북유라시아 21, 동남아·오세아니아 20, 사하라이남 13, 동아시아 6, 유럽 5, 남아시아 3.
  최다 지역 21.6%로 "한 지역 60% 초과" 경보 없음 — 요충 보정은 과하지 않다

**§M13.9 판정: 실패.** 지니(국가 간, 실효)가 100턴 0.2311 → 600턴 0.1989로 **감소**한다.
지도를 실제 지구로 바꿔도 "시간이 편차를 지운다"는 구조는 그대로다. M15 투자층은
보류를 유지하고, 편차 원천 후보(`trade_mult` 강화 / `INFRA_MAX` 지역 의존화 /
프로빈스 자원 특화) 중 하나를 격리 도입해 같은 부호 조건으로 재측정해야 한다.

## 11. 계측 도구

| 도구 | 용도 |
|---|---|
| `tools/probe_culture.gd` | 문화별 시작·생존·미등장 런, 앵커 적중률 |
| `tools/probe_empire_gap.gd` | 지도별 전쟁 산출량(강화·할양·속국화)과 행정 부하 대조 |

> Godot `SceneTree` 스크립트는 마지막에 반드시 `quit(0)`을 부른다. 빠뜨리면 계산이
> 끝난 뒤에도 프로세스가 CPU 0%로 살아 있어 "배치가 안 끝난다"로 보인다.
