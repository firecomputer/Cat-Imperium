# TUNING M14 — 분리주의도 · 유동 편성 상한 · 전쟁 지지도

제국이 스스로 무너지는 경로를 만든다. 기존 시뮬에는 진압의 누적 비용도,
영토 규모에 따른 편성 마찰도, 전쟁 수행 능력의 정치적 상한도 없었다.

## 1. 넣은 것

| 축 | 수치 | 상수 |
|---|---|---|
| 분리주의도 | 프로빈스 0~1 | `Unrest.SEPARATISM_COMBAT 0.02` / `SEPARATISM_SUPPRESSION 0.25` / `SEPARATISM_DECAY 0.002` |
| 불만 상승률 배율 | ×(1 + sep·1.2) — 상승분에만 | `Unrest.SEPARATISM_UNREST_MULT` |
| 통합 정체 | ×max(1-sep, 0.15) | `EmpireSystem.SEPARATISM_INTEGRATION_FLOOR` |
| 야전군 상한 | clamp(프로빈스/4, 2, 8) | `WarAI.ARMY_CAP_*` |
| 편성 속도 | 지지도 1.0 = 1개/턴, 0.0 = 1개/5턴 | `Military.SPAWN_RATE_MIN 0.2` |
| 동원 속도 | ×lerp(0.5, 1.3, 지지도) | `Military.SUPPORT_MOBILIZE_*` |
| 지지도 감소 | 사상자/force_base × 0.60 | `Military.SUPPORT_LOSS_PER_FORCE` |
| 지지도 회복 | 평시 +1%/3턴, 승전 +10%p | `Diplomacy.WAR_SUPPORT_PEACE_GAIN` / `EmpireSystem.WAR_SUPPORT_VICTORY` |
| 개전 금지선 | 0.40 | `Diplomacy.WAR_SUPPORT_FLOOR` |
| 강제 개전 | 지지도 1.0 유지 15턴 | `Diplomacy.WAR_SUPPORT_CRUSADE_TURNS` |
| 진압 의지 | 정치 고문 평균 0~1, 중립 0.5 | `CharacterSystem.SUPPRESSION_MEAN/DEVIATION` |
| 진압 마찰 | max(sep - 의지, 0) × {주둔 0.5, 전선 4.0} | `WarAI.GARRISON_RELUCTANCE` / `FRONT_RELUCTANCE` |

`Nation.war_weariness` 는 `war_support` 로 완전 대체했다. 전쟁 피로와 전쟁 지지도를
둘 다 두면 `war_appetite` 의 억제항과 `Peace._weariness` 에 같은 압력이 이중으로 걸린다.

## 2. 측정

### 2.1 반란 (`probe_rebel.gd`, 8런 × 300턴)

기준선은 신규 상수를 전부 중립값으로 되돌린 사본이다 (분리주의 0, 상한 고정 4,
편성·동원 배율 1.0, 지지도 게이트 해제).

| 지표 | 기준선 | M14 |
|---|---:|---:|
| 반란전-턴 | 14,122 | 15,566 (+10%) |
| `independence_recognized` | 77 | **94 (+22%)** |
| `annihilated` | 199 | 220 |
| `parent_collapsed` | 39 | 41 |
| 모국 압박 0 인 턴 | 75.3% | 79.0% |
| 모국 압박 ≥ 0.5 인 턴 | 3.2% | **7.0%** |

압박 분포가 양극화됐다. 진압 의지가 낮은 내각은 분리주의가 끓는 땅을 아예 놓고,
높은 내각은 전보다 세게 누른다 — §5 가 의도한 갈림이다.

### 2.2 계측 (`probe_m14.gd`, 8런 × 300턴, earth)

```text
이벤트       : war_declared 611, suppression 11390, rebellion 877,
               rebellion_suppressed 235, nation_died 183, crusade_declared 19
개전 게이트   : blocked_support 860 (blocked_authority 42531, blocked_at_war 25949 다음)
지지도 분포   : 1.0 에 199국, 0.0~0.1 에 35국 (양봉)
분리주의 분포 : ~0 에 7016 프로빈스, ≥0.5 에 130 (1.8%)
army_cap 분포 : 2:161, 3:19, 4:42, 5:55, 6:67, 7:42, 8:39
```

강제 개전은 선전포고 611건 중 19건(3.1%)이다. 지지도 만점이 흔한데도 낮은 이유는
쿨다운·권위·전력비 안전장치를 그대로 통과해야 하기 때문이다 — 의도한 비율이다.

### 2.3 배치 (`batch_sim.gd`, 40런 × 300턴, earth)

| 300턴 | 기준선 | M14 |
|---|---:|---:|
| 실효 국가 잔존률 | 0.996 | **0.976** |
| 최대 영토 (프로빈스) | 42.8 | 44.7 |
| 최대 GDP 점유 | 0.060 | 0.063 |
| 전체 국가 수 | 52.1 | 53.1 |
| 지니(국가 간, 실효) | 0.158 | 0.155 |

### 2.4 배치 600턴 (`batch_parallel.sh`, 12런 × 600턴, earth)

제국 형성이 300~600턴 구간에서 일어난다는 관측에 따라 600턴으로 다시 잰다.

| 600턴 | 기준선 | M14 |
|---|---:|---:|
| 제국 형성 런 (유지 24턴) | 0/12 | 0/12 |
| 최대 영토 (프로빈스, 전체 912) | 43.2 | 44.0 |
| 최대 GDP 점유 | 0.065 | 0.068 |
| 실효 국가 (초기 40.0) | 41.7 | 42.2 |
| 전체 국가 | 61.5 | **68.8** |
| 지니(국가 간, 실효) | 0.177 | 0.177 |

전체 국가 수가 61.5 → 68.8 로 벌어진다. M14 는 세계를 더 잘게 부순다 — 분리주의가
진압된 땅을 다시 반란으로 밀어내기 때문이다. 다만 그 조각들이 실효국가(프로빈스
2개 이상)로 자라지 못해 실효 국가 수는 거의 같다.

## 3. 남은 문제

**이 배치에서는 "강해질수록 강해지는 루프"가 애초에 관측되지 않는다.**
기준선·M14 양쪽 다 제국 형성률 0/40 이고, 최대 영토가 912칸 중 43칸(GDP 점유 6%)이다.
M11 게이트가 요구하는 방향은 오히려 *제국이 더 잘 생기는* 쪽이다.

따라서 §M14 의 세 되먹임은 **작동은 하되 아직 거시 지표를 움직이지 못한다**:

- 분리주의가 0.5 를 넘는 프로빈스가 전체의 1.8% 뿐이다. 반란을 겪은 땅에만 집중되는
  것은 설계대로지만, 그만큼 제국 규모에 대한 압력으로는 약하다.
- `army_cap` 과 편성 예산이 거의 물리지 않는다. `probe_capacity` 기준 전선 부족
  국가-턴의 84.4%가 `MIN_ARMY_TROOPS(200)` 미달로 못 쪼갠 경우이고, 상한에 걸린 것은
  15.6% 뿐이다. 중앙값 국가의 총병력이 200 을 넘지 못하는 한 §2·§3 은 대제국에만 걸린다.

다음 단계에서 볼 것:

1. 제국이 실제로 형성되는 조건에서 재측정. **600턴으로 늘려도 earth 지도 12런에서
   제국 형성 0/12 다 (기준선도 동일).** 최대 영토가 912칸 중 44칸, GDP 점유 6.8% 로
   `EMPIRE_THRESHOLD(0.12)` 근처에도 못 간다. 남은 후보는 노이즈 지도, 더 적은 초기
   국가 수, 개전 압력 상향이다 — §M13.8 재기준선과 함께 잡아야 한다.
2. `MIN_ARMY_TROOPS` 를 국가 규모에 상대화할지 — 지금은 이 상수가 §2·§3 을 가린다.
3. 분리주의 확산: 지금은 교전이 벌어진 칸에만 붙는다. 인접 동문화 프로빈스로
   번지게 할지는 §1 의 효과 크기를 보고 정한다.


## 4. 계측 도구와 실행 시간

M14 작업 중 배치 실행 시간이 병목이 되어 두 가지를 넣었다.

### 4.1 `tools/batch_parallel.sh` — 시드 구간 병렬 실행

`batch_sim.gd` 는 시드 `seed0 + i` 로 런을 독립 생성하고 모든 CSV 행이 `seed` 열을
가진다. 구간을 나눠 프로세스 여러 개로 돌린 뒤 이어붙이면 결과가 **순차 실행과
바이트 단위로 같다** (CSV 5종 검증).

```bash
tools/batch_parallel.sh --runs 12 --turns 600 --jobs 12 --out res://out/x.csv
```

12런 × 600턴: 순차 약 15.6분 → 병렬 2분 47초 (양쪽 A/B 를 동시에 돌린 값).

### 4.2 `tools/probe_profile.gd` — 단계별 소요 시간

600턴 earth 1런 기준. 최적화 전 74.7초 → 후 **56.1초 (-25%)**.

| 단계 | 전 | 후 |
|---|---:|---:|
| `Economy.tick_infra` | 19.35s | **10.13s** |
| `Military.tick` | 16.26s | 14.45s |
| `CharacterSystem.tick` | 10.80s | 10.02s |
| `Economy.tick_production` | 4.26s | **1.12s** |
| `Unrest.tick` | 3.43s | **1.87s** |

고친 것 두 가지. 둘 다 결과 불변을 CSV 대조로 확인했다.

- **`Nation.law_modifier` 캐시** — 값은 국가 단위 상수인데 호출은 프로빈스마다
  일어났다 (`build_yield` 안에서만 프로빈스당 4회). 8개 카테고리 순회를 캐시하고
  법률이 바뀔 때만 비운다. `laws` 직접 대입은 캐시를 상하게 하므로 `Nation.set_law()`
  를 유일한 입구로 두고 기존 대입 5곳을 옮겼다.
- **`Economy.gdp_pc_anchor` 의 곡선 양 끝점** — `infra` 와 무관한 상수인데 호출마다
  `exp()` 두 번으로 다시 구했다. `static var` 로 뺐다 (`const` 는 `exp()` 를 접지 못한다).
