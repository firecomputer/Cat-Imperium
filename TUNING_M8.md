# M8 후 튜닝 기록 (2026-08-28)

M9 착수 전 최소 튜닝. 대상은 인수인계된 두 FAIL — 반란 편중과 인프라/도시 미달.

---

## 0. 먼저: 인수인계 수치가 낡았다

인수인계 내용은 다음 두 가지였다.

- 반란 1171건 vs 정복 32건 — 영토 변동의 97%가 반란
- 최고 인프라 6.35 (목표 7~9), 세계 도시 수 3.2 (목표 15~40) — M7부터 이어진 FAIL

**두 번째는 이미 해결된 상태였다.** 근거:

| 파일 | mtime |
|---|---|
| `out/runs.csv`, `out/war.csv` | 08-27 17:47 |
| `sim/systems/economy.gd` | **08-28 16:31** |
| `sim/systems/diplomacy.gd` | **08-28 16:32** |
| `tools/probe_tune.gd` (신규) | 08-28 16:25 |

CSV 생성 이후에 경제/외교 코드가 수정됐고 배치가 재실행되지 않았다. 같은 코드로 재측정한 결과:

```
최고 인프라 (300턴)  평균 8.72  [8.05, 9.82]  PASS
세계 총 도시 수      평균 25.7  [19, 30]      PASS
```

`sim/` 이 git 미추적(`?? sim/`)이라 무엇이 바뀌었는지 diff 로 확인할 수 없다.
**튜닝을 시작하기 전에 항상 배치를 재실행할 것.**

남은 실제 과제는 반란 축 하나였다. 재측정 기준선에서는 반란 1056 vs 병합 21 로
오히려 악화돼 있었다.

---

## 1. 진단

### 1.1 수도거리 항 단독으로 반란이 확정된다

진단용 프로브(`tools/probe_unrest.gd`)로 불만 drift 를 항별 분해했다.

```
=== drift 항별 평균 (프로빈스-턴 18954 샘플) ===
  occupation   +0.00017
  inflation    +0.00120
  culture      +0.00001
  distance     +0.01244
  exclave      +0.00012
  garrison     -0.00591
  suppression  -0.00727
  cohesion     -0.01195
  net          -0.01117      <- 평균은 음수
  주둔 0 프로빈스 비율 84.0%
```

평균 net drift 는 **음수**다. 즉 평균 프로빈스는 진정되는 중인데도 런당 64건씩
반란이 났다. 원인은 평균이 아니라 꼬리에 있었다.

수도거리별 반란율 (프로빈스-턴 샘플 대비):

| 수도거리 | 샘플 | 반란 | 비율 |
|---|---|---|---|
| 0 | 2880 | 24 | 0.8% |
| 1 | 9881 | 50 | 0.5% |
| 2 | 5148 | 114 | 2.2% |
| 3 | 875 | 80 | **9.1%** |
| 4 | 128 | 48 | **37%** |
| 5+ | 42 | 70 | **>100%** |

`DISTANCE_W 0.01 × DISTANCE_CAP 5` = 최대 +0.05/턴.
감쇠 합계는 cohesion −0.012 + suppression −0.007 + garrison −0.006 ≈ **−0.019**.
거리 3부터 순증으로 돌아서고, 거리 5 이상은 예외 없이 떨어져 나간다.

**귀결: 국가 반경이 2 에 하드캡된다.** 전 프로빈스의 94%가 수도거리 ≤2 에 몰려 있었다.
정복해봐야 반경 밖의 땅은 반란으로 되돌아가므로 **정복 자체가 무의미해진다.**
이것이 외교 게이트보다 상위 원인이었다.

인플레 폭주로 인한 반란(drift ≥ 0.10)은 16%뿐이다. 그쪽은 §0.3 붕괴 나선대로
의도한 동작을 하고 있었다.

### 1.2 개전 게이트에 상수 모순이 있었다

`Diplomacy.debug_gates` (6런 × 300턴 × 40국 = 72000 국가-턴):

```
blocked_at_war   10955
blocked_money    20891 (29%)
eligible         31429
t_ally           77278          <- 최대 병목
t_power          26755
t_candidate      10255
t_appetite_fail  31380 / 31429 (99.8%)
```

- `blocked_money`: `WAR_DEBT_CEILING := 0.5` 이 `BudgetAI.infra_credit_target`
  (0.82~0.98)과 정면충돌한다. 설계대로 인프라에 투자하는 나라는 영구히 개전 불가가 된다.
- `t_ally`: 동맹국은 개전 후보에서 제외되는데, 20런에 동맹이 984건(국가당 2.5)이었다.
  접경국이 전부 동맹이라 칠 상대가 없다.

### 1.3 동맹은 래칫이다

`_consider_alliance` 에 **해지 규격이 없다** (설계서 §11 에도 없음).
300턴 중 한 번만 임계를 넘으면 영구 동맹이다. 따라서 진입 임계는 다이얼이 못 된다 —
올려도 체결 시점만 늦춰지고 최종 개수는 그대로다.

`ALLY_MIN_OPINION` 은 특히 쓸 수 없다. 이력 없는 쌍의 opinion 은 매 턴 0 으로
감쇠해 **정확히 0.0** 이고 `_shared_enemies` 는 드물다. 결과는 절벽이다:

| `ALLY_MIN_OPINION` | 동맹 수 (20런) |
|---|---|
| 0.0 | 984 |
| 3.0 | 25 |
| 10.0 | 2 |

---

## 2. 적용한 변경

### `sim/systems/unrest.gd`

```gdscript
const DISTANCE_W := 0.004     # 0.01 에서
const DISTANCE_CAP := 10.0    # 5.0 에서
```

거리를 확정 사형선고가 아니라 규모 비례 압력으로 바꾼다.
거리 3 → +0.012 (감쇠 −0.019 가 이김, 안정), 거리 6 → +0.024 (주둔 필요),
거리 10 → +0.04 (고문 + 주둔 총동원 필요).

### `sim/systems/diplomacy.gd`

```gdscript
const WAR_DEBT_CEILING := 0.9   # 0.5 에서. BudgetAI 와의 모순 해소
const ALLY_MIN_THREAT := 0.70   # 0.45 에서
const ALLY_MAX := 1             # 신규
```

`_consider_alliance` 에 가드 1줄:

```gdscript
if n.at_war or n.allies.size() >= ALLY_MAX:
	return
```

동맹 수 상한이 실질 다이얼이다 (임계는 래칫이라 무효).

### 버그 수정 — 반란 로그

`spawn_rebellion` 이 진단값을 리셋 **후**에 읽고 있었다.

```gdscript
p.distance_from_capital = 0.0
p.is_exclave = false
var origin_distance := p.distance_from_capital   # 항상 0
```

리셋 전으로 옮기고 `drift` / `garrison` / `inflation` 필드를 추가했다.
이 수정 없이는 §1.1 의 원인 특정이 불가능했다.

### 신규 도구

`tools/probe_unrest.gd` — 불만 drift 항별 분해와 반란 스폰 시점 스냅샷.

---

## 3. 결과

| 지표 | 재측정 기준선 | 최종 | 목표 | 판정 |
|---|---|---|---|---|
| 반란 (20런) | 1056 | **704** | — | −33% |
| 영토 병합 (20런) | 21 | **61** | — | 2.9× |
| 반란 : 정복 | 50 : 1 | **11.5 : 1** | — | 개선 |
| 최고 인프라 (300턴) | 8.72 | 8.43 | 7~9 | PASS |
| 세계 총 도시 수 | 25.7 | 26.2 | 15~40 | PASS |
| 첫 파산 턴 | 160.4 | 138.7 | 100~250 | PASS |
| 최대국 세계 GDP 점유 | 6.5% | 6.5% | <25% | PASS |
| 국경 덩어리 수 | 1.00 | 1.01 | — | PASS |
| 인구 총합 보존 | 0.000% | 0.044% | <0.1% | PASS |
| 문화별 생존 | — | 159~161 각 | 각 10%+ | PASS |
| 동맹 수 | 984 | 468 | — | — |

되돌림 후 3런 검증 배치가 T5 결과와 **바이트 동일**했다. 결정론(§15) 유지 확인.

---

## 4. 시도했다 되돌린 것

기록해 두지 않으면 다음 사람이 같은 실험을 반복한다.

### `ALLY_MIN_OPINION := 10.0`

동맹을 20런에 2건까지 없앴다. 동맹이 사라지면 세 가지가 동시에 사라진다 —
표적 제외(`t_ally`), `deterrence` 억지력, `_defended_power` 방어력 합산.
세계가 상시 전쟁이 되어 다 파산했다.

| 지표 | 전 | 후 |
|---|---|---|
| 대외전쟁 선포 | 100 | 1278 |
| 반란 | 559 | **2361** (악화) |
| 첫 파산 턴 | 136 PASS | **64.7 FAIL** |
| 파산 건수 | 83 | **667** |
| 이자부담/GDP | 0.057 | **0.519** |

반란이 오히려 4배가 된 것은 파산 불만(`Credit.DEFAULT_UNREST 0.25`)과 인플레
때문이다. 전쟁을 늘리면 반란도 같이 는다.

### `WAR_THRESHOLD := 0.12`

`t_appetite_fail` 이 자격 국가-턴의 99.86%를 차지해 내려봤다.

| | 병합 | 반란:정복 | 첫 파산 <100턴 시드 |
|---|---|---|---|
| 0.25 | 61 | 11.5:1 | 7/18 |
| 0.12 | 97 | 9.8:1 | **14/20** |

비율 개선은 미미한데 조기 파산 시드가 2배가 된다. 평균은 112.7 로 PASS 지만
70% 시드가 설계 기준(100~250)을 벗어난다. 이득 대비 비용이 나빠 되돌렸다.

---

## 5. 남은 구조 문제 — 상수로 안 됨

반란:정복을 한 자릿수 아래로 내리려면 셋 다 로직 작업이 필요하다. M9 진행에는
지장이 없다 (§16.2 측정 가능 지표는 전부 PASS).

### 5.1 주둔이 작동하지 않는다

반란이 난 프로빈스의 **93%가 `garrison_ratio == 0`** 이다. `WarAI._fronts` 가
군대를 전선에만 보내므로 설계 §10 의 대항 수단(`-garrison_ratio * 0.04`)이
평시에 영영 발동하지 않는다. 불만을 관리할 실질적 수단이 없다는 뜻이다.

→ 예비 병력을 고불만 프로빈스에 주둔시키는 판단을 `WarAI` 에 추가해야 한다.

### 5.2 반란 진압 경로가 없다

`peace.gd:92`:

```gdscript
if _is_rebel_war(world, war):
	if length >= REBEL_RECOGNITION_TURNS:   # 60
		Diplomacy.end_war(world, war, "independence_recognized")
	return
```

반란전쟁에는 warscore 경로가 없다. 60턴 안에 반란국 **전 영토를 점령**하는 것만이
유일한 승리다. 결과는 독립 559 vs 전멸 106 — **70%가 영구 독립**한다.

→ 반란전쟁에도 warscore 기반 항복을 두거나, `REBEL_RECOGNITION_TURNS` 를
   반란 규모에 비례시켜야 한다.

### 5.3 강화조약 수확이 1 프로빈스다

`ACCEPT_SCORE 35` 에 도달하면 즉시 강화가 성립하므로 예산은 사실상 35 로 고정된다.
프로빈스 비용은 `COST_PROVINCE_BASE 15 + COST_PROVINCE_VALUE 60 × 지분` 이라
7프로빈스 국가의 땅 하나가 약 23 이다. 두 번째는 못 산다.

20런 실측: 강화조약 59건 → 병합 61 프로빈스.

→ 전쟁 빈도를 아무리 올려도 여기가 곱셈 상한이다. §12.2 조항 비용을 손대야 한다.

---

## 6. 재현

```bash
godot4 --headless --path . --script res://tools/batch_war.gd  -- --runs 20 --turns 300 --out res://out/war.csv
godot4 --headless --path . --script res://tools/batch_sim.gd  -- --runs 20 --turns 300 --out res://out/runs.csv
godot4 --headless --path . --script res://tools/probe_unrest.gd -- --runs 6 --turns 300
godot4 --headless --path . --script res://tools/probe_tune.gd   -- --runs 6 --turns 300

python tools/analyze_war.py out/war.csv
python tools/analyze.py     out/runs.csv
```

20런 300턴 기준 배치 하나에 약 2~3분.

`out/` 에 A/B 기록이 남아 있다: `base_sim.csv` / `base_war.csv` (기준선),
`t1s`·`t1w` (거리 항), `t2s`·`t2w` (동맹 전멸), `t3s`·`t3w` (`ALLY_MIN_OPINION 3.0`),
`t4s`·`t4w` (threat 임계), `t5s`·`t5w` (**최종**), `t6s`·`t6w` (`WAR_THRESHOLD 0.12`).

---

## 7. 알려진 잔여 FAIL (경미, 기존 지표 외)

- `analyze.py` 의 `GDP 앵커 상한 초과` 3 샘플. 18만 프로빈스-샘플 중 3건으로,
  도시를 잃거나 법률이 바뀐 직후 `gdp_pc` 가 새 앵커로 수렴하는 몇 턴간의 과도상태다.
  발산이 아니다. M3 기준은 0 을 요구하므로 형식상 FAIL 로 찍힌다.
- `평균 부채/GDP` 판정이 FAIL. GDP 가 거의 0 으로 붕괴한 나라 한둘이 40개국 평균을
  끌어올린 것으로, 판정식(`mean_debt_ratio.abs() < 10`)이 이상치에 취약하다.
  지표 자체보다 판정식 문제에 가깝다.
