# M8.5 실행 기록 (2026-08-28)

`M8_5_REBELLION_REWORK.md` 의 네 변경을 구현하고 20런 × 300턴으로 측정한 결과다.
기준선은 같은 코드로 재실행한 M8 최종(`out/m85_base_war.csv`)이며,
`TUNING_M8.md` 의 수치(반란 704, 병합 61, 11.5:1)를 그대로 재현한다.

---

# 0. 계획서와 어긋난 두 가지 — 먼저 읽을 것

계획서는 이 세계의 두 가지 실측값을 모르는 상태로 쓰였다.
그대로 구현하면 §3~§5 가 전부 죽은 코드가 되므로 착수 전에 확인하고 고쳤다.

## 0.1 반란국은 항상 프로빈스 1개였다

실측(3런 300턴, 113 반란): **살아있는 반란국의 프로빈스 수 최대 = 1.**

`spawn_rebellion` 이 프로빈스마다 새 국가를 세우고, 반란국이 땅을 늘리는 경로가
없기 때문이다. 이 상태에서 계획서를 그대로 구현하면:

| 항목 | 결과 |
|---|---|
| `rebel_origin_provinces` | 크기 1 |
| `territory_score` | 0 또는 55 — 55 가 되는 순간은 반란국이 이미 소멸한 시점 |
| `capital_score` | 수도가 같은 프로빈스라 territory 와 동시에 0 |
| 생존 중 정부 최대 warscore | battle 25점 < 65 → **정부는 warscore 로 절대 못 이긴다** |
| `controlled_ratio` | 생존 중 항상 1.0 → **모든 반란이 65턴에 자동 독립** |

즉 §0.1.D 가 지적한 "타이머만 채우면 독립" 이 이름만 바꿔 되살아난다.

**조치 (사용자 승인): 인접 합류.** `unrest` 1.0 에 도달한 프로빈스 옆에
같은 모국에서 갈라져 나와 아직 교전 중인 반란국이 있으면, 새 국가를 세우지 않고
그 반란국에 합류한다 (`Unrest._adjacent_rebellion` / `join_rebellion`).
후보가 여럿이면 국가 id 가 낮은 쪽 — 결정론 유지 (§15).

실측 결과 반란의 **42%** 가 합류이고 `origin_provinces` 는 평균 1.38, 최대 7 이 되었다.

## 0.2 계획서의 주둔 규모가 이 시뮬과 20배 어긋난다

계획서 §2.6 은 `max(500, 인구 × 0.015)` 를 제시하고 "인구 20,000 → 500" 을 예로 든다.
실측 중앙값은 **프로빈스 인구 4,145, 국가 총병력 139** 이다.
그대로 쓰면 프로빈스 하나를 채우는 데 국가 전군의 3.6배가 필요해
치안 목표가 있는 국가-턴의 76% 가 예산 부족으로 탈락했다.

형태(하한 + 인구 비례)는 유지하고 배율만 맞췄다.

```gdscript
const GARRISON_TROOPS_PER_POP := 0.006   # 계획서 0.015
const GARRISON_MIN_REQUIRED := 15.0      # 계획서 500
```

중앙 프로빈스 필요 병력 25 = 평시 치안 예산(총병력 25%)의 약 3/4 이다.
분견대 하한도 `MIN_ARMY_TROOPS`(200, 야전군 분할 기준) 대신 5 로 두어
부분 주둔을 허용한다.

---

# 1. 구현한 것

## 1.1 M8.5-A — 치안 주둔 (`sim/ai/war_ai.gd`)

야전군 상한 `MAX_ARMIES`(4) 는 그대로 두고, **치안 분견대를 별도 상한
`GARRISON_MAX_DETACHMENTS`(6) 으로 추가 편성**한다 (사용자 승인).
4개로는 국가당 프로빈스 7개 중 4곳밖에 못 눌러 §9 목표를 구조적으로 못 맞춘다.

- `garrison_need(p)` = unrest + 월경지 0.15 + min(수도거리×0.025, 0.20) + 도시 0.15
- `GARRISON_NEED_MIN` 0.35 미만은 후보 제외
- 예산 = 총병력 × 25%(평시) / 8%(전시). 분견대는 **기존 야전군에서 떼어내며**
  새 병력을 만들지 않는다 (§13). 야전군 하나에서 절반 넘게 떼지 않는다.
- `_organize` 의 평시 분할 항(`n.provinces.size() / 2`)은 제거했다.
  요충지 점거는 이제 분견대가 맡는다.

### 발견 A — 순위 재정렬로 분견대가 영영 도착하지 못한다

`held` 를 매 턴 바뀌는 위험 순위로 정렬하면 순위가 한 칸 흔들릴 때마다
해산·재편성이 반복된다. 실측 **배정 62% 중 도착 9%**.
오래된 분견대부터 예산을 주도록(id 순) 바꿔 자리를 지켜준다.

### 발견 B — 반란의 75%가 육로로 닿지 않는 섬에서 난다

이것이 M8.5 전체의 지배 변수다.

| 측정 | 값 |
|---|---:|
| `land_neighbors` 가 빈 프로빈스 | 전체의 8.8% |
| 그 프로빈스 소유국이 해당 해역 제해권 보유 | **1%** |
| 전체 반란 중 그런 프로빈스에서 발생 | **75%** |

섬은 `garrison_need` 가 항상 0.35 을 넘어 1순위 후보가 되지만
분견대가 걸어서 도착할 수 없다. 초기 구현에서는 그 분견대가 출발지에
얼어붙은 채 치안 예산을 통째로 묶어 **치안 비율이 0.2%** 까지 떨어졌다.

**조치 (사용자 승인): 도달 불가 자국 영토에는 현지 주둔으로 직접 배치.**
주둔군은 행군하는 야전군이 아니라 현지 주둔 병력이다. 병력은 여전히
치안 예산에서 떼므로 신규 생성은 없다. 다만 **전시·교전 중·공성 중에는 금지**해
포위된 거점에 병력이 순간이동하는 우회로가 되지 않게 했다
(`WarAI._can_post_directly`).

## 1.2 M8.5-B — 반란전 warscore (`sim/systems/peace.gd`)

계획서대로 territory 55 + battle ±25 + capital 20.
`parent_losses` / `rebel_losses` 는 새로 두지 않았다 — 모국은 반란전의 공격
진영이므로 기존 `attacker_losses` / `defender_losses` 가 곧 그 값이다 (§7).

`_is_rebel_war()` 의 "참전국 중 반란국이 있으면 반란전" 판정을 `war.is_rebel_war`
플래그로 바꿨다. 부수 효과로 **독립국·제3국이 반란국에게 선전포고한 일반전이
이제 정상적으로 강화된다** — 이전에는 영원히 끝나지 않았다.

## 1.3 M8.5-C — 재통합 (`sim/systems/unrest.gd`, `peace.gd`)

- `_resolve_rebel_defeat()` 은 `rebel_origin_provinces` 중 **현재 반란국 소유**인
  것만 일괄 반환한다. 제3국이 병합한 땅은 건드리지 않는다 (§4.4).
- 영토 반환보다 **먼저** 전쟁을 닫는다. 마지막 프로빈스를 돌려받는 순간
  반란국이 소멸하면서 `kill_nation` 이 종료 사유를 `annihilated` 로 덮어쓰기 때문이다.
- 전장 탈환(`reclaim_from_rebel`)과 일괄 반환이 같은 재통합 경로를 쓴다:
  `unrest ≤ 0.35`, `rebellion_grace_turns = 16`.

### 왜 유예가 15가 아니라 16인가

전장 탈환은 파이프라인 10단계(`Military`), 불만은 11단계다.
재통합된 턴에 유예가 이미 1 소모되므로 15로 두면 §9의 "진압 후 15턴 내 재반란 0%"
가 정확히 경계에서 깨진다(실측 46건).

추가로 유예 감소를 `tick_province` 안이 아니라 `Unrest.tick` 끝의 프로빈스 루프로
옮겼다. 안에서 깎으면 같은 턴에 소유가 바뀐 프로빈스가 두 번 세어져 유예가
한 턴 더 짧아진다.

## 1.4 M8.5-D — Recognition (`sim/systems/peace.gd`)

`REBEL_RECOGNITION_TURNS`(60) 를 제거하고 계획서 §5.3 공식을 그대로 넣었다.
독립 시 `is_rebel = false` 로 두어 이후 일반 국가로 작동한다 (§5.5).

`REBEL_PARENT_VICTORY_SCORE` 만 65 → **50** 으로 내렸다.
이 세계의 `origin_provinces` 는 평균 1.38 이라 65에서는
territory(55×비율) + capital(20) 조합이 거의 만들어지지 않아
**§3 경로가 20런 동안 한 번도 발동하지 않았다**(전부 전멸 판정).
50에서는 절반 탈환 + 전투 우위로 도달할 수 있어
"전 영토 점령 없이 진압"이 실제로 일어난다(20런 4건).

`RECOGNITION_CONTROL_GAIN` 은 0.9 로 낮춰도 봤으나 독립률이 72%→68% 로만 움직이고
지속시간 중앙값만 64→79턴으로 늘어 계획서 초기값 1.2 를 유지했다.

---

# 2. 결과 — §9.1 반란 지표

6런 × 300턴 (`tools/probe_rebellion.gd`).

| 지표 | 목표 | 결과 | 판정 |
|---|---|---:|---|
| 반란 발생 프로빈스 중 주둔 0 비율 | 93% → 60% 이하 | **57%** | PASS |
| 반란전 지속시간 중앙값 | 20~90턴 | **64턴** | PASS |
| 150턴 이상 반란전 | <5% | **0%** | PASS |
| 진압 후 15턴 내 재반란 | 0% | **0%** | PASS |
| 반란전 정부 진압률 | 30~60% | **28%** | FAIL |
| 독립 인정률 | 30~60% | **72%** | FAIL |
| 평시 전체 군대 중 치안 비율 | 5~25% | **1.1%** | FAIL |
| 전쟁 중 전체 군대 중 치안 비율 | 0~10% | **1.2%** | PASS |
| 진압 후 50턴 내 재반란 | 측정만 | 79% | — |

## 2.1 세 FAIL 의 원인은 하나다 — 섬

진압률·독립률을 **육로가 닿는 반란과 섬 반란으로 나누면 그림이 뒤집힌다.**

| 구분 | 반란전 | 진압 | 독립 |
|---|---:|---:|---:|
| 본토 (육로 도달 가능) | 41 | **24 (59%)** | **17 (41%)** |
| 섬 (`land_neighbors` 없음) | 44 | 0 (0%) | 44 (100%) |
| 합계 | 85 | 24 (28%) | 61 (72%) |

**본토 반란은 목표 대역(30~60%) 한가운데에 들어간다.**
섬 반란은 진압군이 물리적으로 도달할 수 없어 100% 독립한다 — 상륙에는 제해권이
필요한데 소유국이 그 해역을 쥐는 비율이 1% 다.

치안 비율 1.1% 도 같은 이유다. `GARRISON_NEED_MIN` 0.35 를 넘는 프로빈스가
국가-턴의 19% 뿐이라 대부분의 나라는 주둔할 대상 자체가 없다.
목표가 있는 나라는 총병력의 5~18% 를 실제로 묶는다.

**이 세 지표는 M8.5 의 네 변경으로는 더 못 움직인다.**
`Naval` 의 제해권 획득 조건을 손대거나 섬의 `EXCLAVE_W`·`DISTANCE_W` 를 다시
튜닝해야 하는데, 둘 다 §13 과 §16 이 이 마일스톤에서 금지한 영역이다.
M9 이후 과제로 남긴다.

---

# 3. 결과 — §9.2 M8 회귀

20런 × 300턴 (`out/m85_base_*` vs `out/m85_d_*`).

| 지표 | 목표 | M8 기준선 | M8.5 | 판정 |
|---|---|---:|---:|---|
| 최고 인프라 | 7~9 | 8.72 | **8.61** | PASS |
| 세계 도시 수 | 15~40 | 25.7 | **26.6** | PASS |
| 인구 총합 보존 오차 | <0.1% | 0.044% | **0.050%** | PASS |
| 문화별 생존율 | 각 10% 이상 | 99~100% | **100%** | PASS |
| 300턴 최대국 GDP 점유 | <70% | 6.5% | **6.6%** | PASS |
| 첫 파산 발생 턴 | 100~250 | 138.7 | **99.2** | FAIL (경계) |
| 국경 덩어리 수 | 현 수준 유지 | 1.01 | **1.30** | 주의 |

## 3.1 반란 축

| | 기준선 | M8.5 |
|---|---:|---:|
| 반란 발생 | 704 | **543** |
| 독립 인정 | 559 | **209** |
| 전멸 진압 | 106 | 101 |
| warscore 진압 | 0 | **4** |
| 영토 병합 | 61 | 40 |
| 반란 : 정복 | 11.5 : 1 | 13.6 : 1 |

독립 인정이 **63% 줄었다**. 반란 자체도 23% 줄었는데, 이는 반란 빈도를 직접
건드려서가 아니라 주둔이 실제로 불만을 눌렀기 때문이다 (§1 원칙 유지).

## 3.2 첫 파산 99.2턴 — 무엇이 당겼는가

지표 정의에 따라 값이 갈린다.

| 측정 | 기준선 | M8.5 |
|---|---:|---:|
| `*_defaults.csv` (반란국 제외, TUNING_M8 기준) | 138.7 | **99.2** |
| `defaults_cum` 첫 발생 (반란국 포함) | 165.8 | **121.0** |

두 정의 모두 앞당겨졌고, 반란국 제외 기준은 목표 하한 100 을 0.8턴 밑돈다.

원인은 주둔군 비용이 아니다. **병력 총합은 164,494 → 166,535 로 사실상 같고,
분견대는 기존 병력을 떼어 쓸 뿐 새로 뽑지 않는다.** 실제 경로는 이렇다.

```text
반란전 선포 704 → 354
→ n.at_war 로 묶여 있던 국가-턴 감소
→ _consider_war 의 "이미 전쟁 중이면 개전 불가" 게이트가 풀림
→ 대외전쟁 선포 228 → 264 (+16%), 백지평화 91 → 157 (+73%)
→ BudgetAI.military_share 가 ×1.25(반란 진압)에서 ×1.5(대외전쟁)로
→ 군사비 증가 → 파산 앞당김
```

즉 **M8.5 가 의도대로 작동한 결과**다 (계획서 §10.2·§10.3).
되돌리려면 `WAR_THRESHOLD` 나 `MILITARY_WAR_MULT` 를 다시 만져야 하는데
§13 이 명시적으로 금지한 방향이라 이 마일스톤에서는 손대지 않았다.
M9 착수 전에 이 한 지표를 되돌릴지 결정해야 한다.

## 3.3 월경지 4 → 239, 국경 덩어리 1.01 → 1.30

기준선의 1.01 은 국경이 깔끔해서가 아니라 **떨어져 있는 땅이 예외 없이
반란으로 떨어져 나갔기 때문**이었다. 이제 섬·월경지가 주둔으로 유지되므로
그대로 남는다. 평균 영토도 6.59 → 6.91 로 늘었다.

수치는 나빠 보이지만 방향은 설계 의도에 맞다 — 다만 §9.2 의 "현 수준 유지" 는
못 지켰으므로 기록해 둔다.

---

# 4. 결정론

```text
out/m85_det_a.csv                == m85_det_b.csv                (14,466 B)
out/m85_det_a_defaults.csv       == m85_det_b_defaults.csv       ( 5,984 B)
out/m85_det_a_credit_events.csv  == m85_det_b_credit_events.csv  (35,189 B)
out/m85_detw_a.csv               == m85_detw_b.csv               ( 7,668 B)
out/m85_detw_a_events.csv        == m85_detw_b_events.csv        (10,631 B)
```

동일 seed 3런 300턴을 두 번 돌려 **바이트 동일**.
전역 `randf()` 추가 없음. Dictionary 순회 순서 의존 없음 —
주둔 후보는 `need → unrest → province.id`, 인접 반란국은 국가 id 최소,
분견대 예산 배분은 army id 순으로 정렬한다.

---

# 5. 회귀 테스트

`tools/test_rebellion.gd` — 18개, 계획서 §14 전 항목.

```text
주둔        불만 우선 / id 동점 / 평시 예산 상한 / 전시 예산 상한 /
            도달 불가 영토는 현지 배치, 전시에는 금지
warscore    100% 탈환 = 55 / 수도 +20 / 동일 손실 = 0 /
            전 영토 점령 없이 임계 초과 시 진압
재통합      반란국 소유만 반환 / 제3국 소유 불변 / unrest ≤ 0.35 /
            유예 16턴 동안 재봉기 없음 / 유예 중에도 drift 계속
recognition 전 영토+수도 상승 / 절반은 더 느림 / 30% 미만 감소 /
            5% 이하 급락 / 100 도달 시 독립 / 시간만으로는 0
```

기존 `test_war` / `test_military` / `test_credit` / `test_characters` 전부 PASS.
`test_war` 는 `Unrest.GARRISON_FULL` 이 `required_garrison()` 으로 바뀐
한 줄만 고쳤다.

---

# 6. §15 완료 기준 대조

## 기능 — 전항 충족

- [x] AI 가 평시 고불만 프로빈스에 실제 병력을 주둔시킨다
- [x] `garrison_ratio` 가 반란 억제에 실제로 쓰인다 (반란 704 → 543)
- [x] 전 영토 점령 없이 군사적으로 진압할 수 있다 (`rebellion_suppressed` 4건)
- [x] 진압 성공 시 잔여 원영토가 일괄 재통합된다
- [x] 재통합 직후 즉시 재반란하지 않는다 (15턴 내 0%)
- [x] 고정 60턴 독립 판정 제거
- [x] 영토를 유지할수록 recognition 상승
- [x] 영토를 잃은 반란은 시간만 버텨 독립할 수 없다

## 밸런스 — 4/6

- [x] 반란전 지속시간 중앙값 64턴
- [x] 150턴 이상 교착 0%
- [x] 반란 발생 시 주둔 0 비율 57%
- [ ] 진압률 28% / 독립률 72% — **본토만 보면 59% / 41% 로 대역 안**, 섬이 원인
- [ ] 첫 파산 99.2턴 (목표 100~250) — 대외전쟁 증가의 귀결

## 기술 — 전항 충족

- [x] 동일 seed 바이트 동일
- [x] 신규 RNG 의존 없음
- [x] 일반 국제전 평화협상 동작 변경 없음 (반란국 상대 일반전이 이제 정상 종료되는
      것은 버그 수정이다)
- [x] `out/` 에 `m85_base_*` / `m85_d_*` 보존
- [x] 본 문서

---

# 7. M9 이전에 결정할 것

1. **섬 반란을 어떻게 할 것인가.** 지금은 100% 독립이 확정이다.
   `Naval` 의 제해권 획득을 완화해 상륙 진압을 가능하게 하거나,
   섬의 불만 항을 다시 튜닝해야 한다. 둘 다 M8.5 범위 밖이라 손대지 않았다.
2. **첫 파산 99.2턴을 되돌릴 것인가.** 되돌리려면 `MILITARY_WAR_MULT`(1.5) 나
   `WAR_THRESHOLD`(0.25) 를 만져야 한다. §13 은 이 방향을 금지했다.
3. **국경 덩어리 1.30 을 받아들일 것인가.** 떨어져 있는 땅을 유지할 수 있게 된
   결과이므로 지표 정의를 바꾸는 쪽이 맞을 수 있다.

# 8. 계획서 §12 A/B 절차와 다르게 한 점

`m85_a_*` / `m85_b_*` / `m85_c_*` 단계별 20런 CSV 는 남기지 않았다.
A 단계에서 주둔이 작동하지 않는 원인이 여섯 겹(예산 규모 → 순위 재정렬 →
도달 불가 → 분견대 하한 → 유예 이중 감소 → 섬)이라, 20런 배치보다
`tools/probe_rebellion.gd` 6런 프로브로 원인별 계측을 반복하는 편이 빨랐다.
최종 A/B 비교는 `m85_base_*` ↔ `m85_d_*` 로 남아 있다.
