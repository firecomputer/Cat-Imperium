# M9 실행 기록 (2026-08-28)

`CAT_IMPERIUM_DESIGN.md` §14와 M9 체크리스트의 시장·헥스 렌더러·HUD·차트를
구현하고 실제 렌더링과 헤드리스 회귀로 확인한 기록이다.

## 0. 착수 시 결정

사용자 결정에 따라 `TUNING_M8_5.md` §7의 세 항목은 M9에서 건드리지 않는다.

- 섬 반란의 100% 독립
- 첫 파산 평균 99.2턴
- 국경 덩어리 평균 1.30

세 항목은 M10 튜닝으로 넘긴다. M9는 실제 관전·투자 화면을 만들어 이후 튜닝을
눈으로 확인할 수 있게 하는 데 집중한다.

기존 `Main.tscn` 삭제는 의도된 초기화로 보고 새 진입 화면을 만들었다.

## 1. 시장과 최소 포트폴리오

### 1.1 가격

설계서 §14의 세 공식을 그대로 구현했다.

```text
국채 = 100 × 신용도 × (1 - clamp(인플레, 0, 0.9))
프로빈스 지분 = GDP / 1000 × (1 - 불만 × 0.7) × 보급
인물 후원 = 최적 보직 점수 × 남은 수명 계수 × 0.5
```

`growth_headroom`도 §14.1의 앵커 대비 격차로 제공한다.

별도 가격 발견, 주문장, 시장 충격, 유동성, 공매도는 만들지 않았다. 설계서 본문의
“소량 노이즈”도 이번 최소 구현에서는 넣지 않았다. 표시된 가격이 기존 시뮬 신호와
정확히 대응하고 신규 RNG 스트림이 생기지 않도록 한 선택이다.

### 1.2 플레이어 상태와 거래

`PlayerPortfolio`는 `WorldState` 안의 `RefCounted` 상태다.

- 초기 현금: 25,000
- 거래 단위: 정수
- 상품: 국채, 프로빈스 지분, 인물 후원
- 매수·매도: 현재 표시가로 즉시 체결
- 행동 로그: 턴, 매수/매도, 상품 종류, ID, 수량, 가격
- 기록: 현금, 순자산, 누적 배당, 거래 수

별도 플레이어 노드나 뷰 상태에 자산을 두지 않았으므로 동일 턴의 동일 행동 로그를
재생하는 구조를 유지할 수 있다.

### 1.3 배당

가격 변화만으로는 최소 투자 게임의 현금 수익이 성립하지 않으므로 세 상품에 단순한
현금흐름을 붙였다.

```text
국채: 현재 채권 가격 × 국가 금리
      파산 조정 기간에는 지급 정지

프로빈스: 현재 지분 가격 × 0.5% / 턴

인물 후원: 실제 보직 점수 × 2% / 턴
           무보직·사망 인물은 지급 없음
```

부실 국채 이자를 액면 100 기준으로 주면 가격 5짜리 채권이 한 턴에 약 27을 지급하는
무위험 차익이 생긴다. 그래서 국채 배당은 현재 시장가 기준으로 계산한다.

## 2. 새 관전 화면

`Main.tscn`과 `view/`를 새로 만들었다.

### 지도

- 100×150 odd-r 헥스 지도
- 국가, 경제, 인프라, 불만, 보급의 다섯 레이어
- 수도·도시 표시
- 점령지와 월경지 색조 구분
- 클릭 선택, 마우스 휠 확대, 우클릭/가운데 클릭 이동
- 프로빈스 툴팁

### HUD

- 새 시드 생성
- 일시정지, 한 턴 진행, 1×/4×/16× 배속
- 현재 턴, 생존 국가, 활성 전쟁, 세계 GDP
- 플레이어 현금, 순자산, 수익률
- 선택 국가의 경제·재정·군사·법률·고문 정보
- 선택 프로빈스의 성장 여력·불만·보급

### 시장과 기록

- 가격순 상품 목록과 보유 표시
- 수량 지정 매수·매도
- 상품별 신용/인플레, 성장 여력, 인물 능력치 정보
- 보유 자산과 누적 배당
- 전쟁·동맹·반란·파산·영토 할양·도시 탄생·거래 사건 기록
- 국가 GDP/부채, 포트폴리오 순자산/현금, 세계 GDP 차트

시뮬 코어가 `Node`를 참조하지 않게 하기 위해 EventBus 오토로드를 SimClock에서
호출하지 않았다. 뷰 호스트가 `world.events`를 커서로 단방향 소비한다. 배치 도구는
기존처럼 전체 이벤트 배열을 그대로 분석할 수 있다.

## 3. 검증

### 3.1 신규 테스트

`tools/test_market.gd`

- 설계 가격 공식
- 매수·매도와 잔고
- 잔액 부족 거부
- 세 상품 배당 조건
- 사망 인물 가치 0과 포트폴리오 정리
- 턴별 현금·순자산 기록
- 실제 생성 월드에서 세 상품 보유 후 20턴 통합 실행

`tools/test_view.gd`

- 메인 씬 세계 생성
- 지도와 동일 `WorldState` 연결
- 초기 수도 선택
- 시장 목록 생성
- UI 매수
- 한 턴 진행과 포트폴리오 차트 기록

결과:

```text
market tests: PASS
view tests: PASS
credit tests: PASS
character tests: PASS
military tests: PASS
war tests: PASS
rebellion tests: PASS
```

법률 문화별 출력도 오류 없이 완료했다.

### 3.2 결정론과 M8.5 회귀

동일 seed 3런 × 100턴을 두 번 실행해 기본 CSV, 파산 CSV, 신용 이벤트 CSV가
각각 바이트 동일했다.

seed 1 × 300턴 결과는 M8.5 최종 파일에서 같은 seed만 추린 결과와 세 파일 모두
SHA-256이 일치했다.

```text
sim           09b438bf400c4ae913d01849d4fe8e561b4952cf3971637c97a2c8815e7b0ea9
defaults      1fd3c1bf0f3a0ed24d37f525f28c36e74f08bb75de9e7ca5dceae09d260f74cb
credit events cb5b621bf7c62f551f3ecdd6aa2536624031e726559887901c5b4b506117c81a
```

> **폐기됨** — M9.2 해역 도입이 시뮬을 바꿨다. 새 기준선은 §18 이다.

즉 포트폴리오를 조작하지 않은 관전 세계는 M8.5와 완전히 같은 결과를 낸다.

### 3.3 실제 렌더

X11 OpenGL 렌더로 1280×720 화면을 캡처해 글자 겹침과 패널 배치를 확인했다.

- `out/m9_view.png` — 국가 상세와 정치 지도
- `out/m9_market.png` — 국채 매수 후 시장·포트폴리오 화면

두 파일은 다른 분석 산출물과 같이 `out/`에 있으며 git에서는 제외된다.

## 4. M9 완료 기준 대조

- [x] `market.gd`
- [x] 실제 매수·매도 가능한 최소 포트폴리오
- [x] 헥스 렌더러
- [x] 관전 HUD
- [x] 국가·시장·세계 차트
- [x] 헤드리스 배치와 기존 테스트 유지
- [x] 실제 렌더 화면 검증

“관전만으로 재미가 성립하는가”는 장시간 플레이 감상의 주관 평가가 남지만, 이를
검증할 지도 레이어·사건 기록·상세 정보·배속·투자 루프는 모두 동작한다.

## 5. M10으로 넘길 것

1. 섬 반란·첫 파산·국경 덩어리의 M8.5 잔여 지표
2. 초기 자금과 세 상품 배당률의 다중 seed 수익률 튜닝
3. 문화·상품별 투자 성과 편중 확인
4. 장시간 관전 시 사건 밀도와 차트 가독성 조정
5. 정보 비대칭, 공매도, 반란 후원 등 §14.2 확장은 2차 이후 범위 유지

---

# M9.1 관전 화면 개선 (2026-08-28)

M9 완료 후 실제 관전에서 나온 네 가지 불만을 처리한 기록이다.

1. 지도 UI 렉
2. 1배속이 너무 빠름
3. 기록만 봐서는 어느 국가인지 알 수 없음
4. 진행중인 전투가 지도에 안 보임

시뮬 수치·RNG 소비 순서는 건드리지 않았다. §3.2 회귀 해시는 그대로다 (§10).

## 6. 지도·UI 성능

### 6.1 원인 두 가지

**(a) 타일마다 드로우 호출.** `map_renderer.gd` 가 redraw 마다 육지 5,000 헥스에
`draw_colored_polygon` + 안티에일리어스 `draw_polyline` 을 냈다. redraw 1회 = 1만 번의
프리미티브.

**(b) 한글 폰트 대체 탐색.** 테마에 폰트를 지정하지 않아 기본 폰트를 썼는데 여기에는
한글이 없다. RichTextLabel 이 글리프마다 시스템 폰트를 뒤지느라 **한 줄에 약 1.7 ms** 가
들었다. 국가 상세 패널은 20줄이라 갱신 한 번에 28 ms 였다. 같은 내용을 라틴 문자로 바꾸면
40줄에 1.2 ms 다.

(b) 는 (a) 만큼이나 컸고, 화면 전체(툴팁·차트·목록)에 걸려 있었다.

### 6.2 렌더러 재작성

- 정점·인덱스·경계 변을 `set_world()` 에서 1회 캐시한다.
  헥스 5,000개 = 정점 30,000, 프로빈스 경계 변 7,031, 격자 변 16,534.
- 매 프레임 `RenderingServer.canvas_item_add_triangle_array` 1회 + `draw_multiline`
  몇 개만 낸다.
- `_base`(지형·경계) / `_overlay`(도시·전쟁·선택) 두 계층으로 나눠 마우스 호버가
  지형을 다시 그리지 않게 했다.
- 색은 턴이 바뀔 때만, 그것도 **직전 색과 다른 프로빈스만** 다시 칠한다.
- 국경·전선·점령 외곽은 지배국이 바뀐 턴에만 다시 만든다.
- 헥스 한 칸이 9픽셀보다 작게 보이면 벌집 격자선은 어차피 픽셀 이하라 통째로 건너뛴다.
- 반대로 축소 시 선이 점선처럼 부서지지 않도록 선·마커에 화면 픽셀 최소 굵기를 줬다.

### 6.3 뷰 호스트

- 테마에 `SystemFont`(Noto Sans CJK KR 계열)를 지정했다. (b) 의 해결.
- 국가 상세의 `[table]` BBCode 를 평문 줄로 바꿨다. 표 파싱은 그 자체로 비쌌다.
- 매 프레임 갱신을 상단 바(턴·국가·전쟁·GDP·현금)로 한정하고, 무거운 패널은
  최소 0.25초 간격으로만 돌린다.
- 기록 로그는 140줄을 매번 재파싱하지 않고 새 줄만 `append_text` 한다.
- 차트 기록은 2,000개에서 잘라 낸다. 차트는 마지막 140개만 쓴다.
- 시장 목록 정렬이 비교자 안에서 가격을 다시 계산하던 것을 한 번만 계산하도록 고쳤다.
- 소비가 끝난 `world.events` 는 4,000개마다 잘라 낸다.
- 한 프레임에 몰아치는 틱 수 상한을 24에서 2로 낮췄다. 최고 배속도 초당 10.7틱이라
  2면 충분하고, 프레임이 한 번 밀렸을 때의 따라잡기 히칭이 사라진다.

### 6.4 측정 (1280×720, X11 OpenGL)

지도 드로우만 따로 잰 값:

```text
개선 전  94.41 ms/frame   (10.6 fps)
개선 후   0.0x ms/frame   (프레임 오버헤드 5.98 ms 와 구분되지 않음)
```

실제 관전 화면 전체:

```text
1배속   평균 164.6 fps   최악 프레임 21.7 ms
32배속  평균 154.0 fps   최악 프레임 52.4 ms   (10.6턴/초)
```

턴이 진행되는 프레임의 내역 (120턴 세계):

```text
sim tick   17.0 ms      ← 시뮬 자체. 뷰와 무관하다
events      0.6 ms
지도 갱신   colors 0.36 / edges 1.56 / war 0.75 ms
패널        0.35 ms
드로우      약 1.9 ms
```

남은 비용은 시뮬 틱이며 뷰 쪽 여유는 충분하다.

## 7. 배속

1배속을 **1턴 3초**로 맞췄다.

```gdscript
const BASE_TICKS_PER_SECOND := 1.0 / 3.0
const SPEEDS := [1, 2, 4, 8, 16, 32]      # 최고 ≈ 10.7턴/초
```

단축키는 `1`~`6`. 버튼 툴팁에 "n 키 · 1턴 x.x초" 를 적었다. 버튼이 3개에서 6개로 늘어
1280 폭에서 오른쪽 자산 표시가 잘렸으므로 상단 바 간격과 배속 버튼 여백을 줄였다.

## 8. 전쟁 오버레이

지도 레이어(국가/경제/인프라/불만/보급)와 무관하게 항상 그린다. 툴바의 "전쟁 표시" 로
끌 수 있다. 매 턴 1회만 계산한다.

| 표시 | 출처 |
|---|---|
| 군대 마커 (국가색 원, 병력 로그 스케일) | `world.armies` 의 프로빈스별 국가별 병력 |
| 교전 (붉은 링 + 교차 검, 맥동) | 한 프로빈스에 `Diplomacy.are_at_war` 인 두 국가 군대 공존 |
| 공성 (진행도 링) | `p.siege_by_nation` / `p.siege_progress` |
| 점령지 외곽 (점령국 색) | `p.occupied_by_nation` |
| 전선 (굵은 붉은 선) | 양쪽 지배국이 교전중인 프로빈스 경계 |
| 해전 (분지 중심 함대 마커) | `world.fleets` 의 분지별 국가별 함선 |

프로빈스 툴팁에도 점령국·공성 진행도·주둔 병력과 사기를 붙였다.

## 9. 기록에서 지도로

### 9.1 국명

`Nation.name` 을 추가하고 `data/names/*.json` 에 `nation_stems` / `nation_titles` /
`nation_separator` 를 넣었다. 문화별 12 어간 × 5 칭호 = 60 조합이고 문화당 국가는 8개다.
중복이 나면 재시도하고 그래도 겹치면 서수를 붙인다.

```text
00 Nakhonrat 번왕국 (샴)      04 한벌도호부 (코리안숏헤어)
02 Argenteuil 공국 (치즈 태비)  08 Volkhov 차르국 (러시안블루)
```

반란국은 난수를 쓰지 않고 모국에서 파생한다 (`"%s 반란군"`). 반란 시점에 난수를 뽑으면
다른 시스템의 난수열이 밀려 결정론이 깨진다.

이름 부여는 **새 RNG 스트림 `nation_names`** 로만 뽑는다. `RngPool` 은 키별 독립 시드이므로
기존 스트림의 순열이 그대로 유지된다 (§10 에서 확인).

### 9.2 클릭·검색

- 기록의 국가·지역이 전부 `[url=n:*]` / `[url=p:*]` 링크다. 누르면 지도가 그 자리로
  이동하고 관련 국가 영토가 몇 초간 흰 외곽선으로 강조된다. 마우스만 올려도 미리 강조된다.
- 전쟁·동맹 줄의 머리말은 `[url=w:a,b]` 라 양쪽을 한 번에 강조한다.
- `F` 키로 검색창을 연다. 국명 부분 일치와 프로빈스 번호를 받아 Enter 로 이동한다.
  `Esc` 로 닫는다.
- 기록에 없던 사건을 추가했다: 점령·탈환·상륙·해전·평화 조약·속국화·참전.
- `battle_resolved` 는 교전중인 프로빈스마다 매 턴 뜬다. 그대로 찍으면 기록이 잠기므로
  프로빈스별로 **전투 개시 / 전투 종료 두 줄**로 줄였다.

## 10. 검증

### 결정론

```text
seed 1 × 300턴
sim           09b438bf400c4ae913d01849d4fe8e561b4952cf3971637c97a2c8815e7b0ea9
defaults      1fd3c1bf0f3a0ed24d37f525f28c36e74f08bb75de9e7ca5dceae09d260f74cb
credit events cb5b621bf7c62f551f3ecdd6aa2536624031e726559887901c5b4b506117c81a
```

> **폐기됨** — M9.2 해역 도입이 시뮬을 바꿨다. 새 기준선은 §18 이다.

§3.2 의 M8.5 회귀 값과 세 파일 모두 일치했다. seed 1~3 × 100턴을 두 번 돌린 결과도
바이트 동일했다.

### 테스트

```text
market tests: PASS      military tests: PASS
view tests: PASS        war tests: PASS
credit tests: PASS      rebellion tests: PASS
character tests: PASS
```

`tools/test_view.gd` 에 국명 유일성, F 검색 이동, 전쟁 오버레이 마커 수(실제 공성·교전
수와 일치), 기록의 지도 링크 존재를 단언으로 추가했다.

### 실제 렌더

1280×720 X11 OpenGL 캡처를 `out/` 에 남겼다 (git 제외).

```text
m9_1_map_full.png   전 지도 · 전선/교전 마커
m9_1_map_war.png    교전 지역 확대 · 공성 링 · 국가 강조
m9_1_map_gdp.png    경제 레이어에서도 전쟁 오버레이 유지
m9_1_log.png        기록의 링크
m9_1_market.png     국명이 붙은 시장
m9_1_search.png     F 검색
```

## 11. 남은 것

M9 §5 목록은 그대로 M10 으로 간다. 여기에 더해:

1. 국경이 바뀌지 않은 턴에는 경계 재계산(1.56 ms)도 건너뛸 수 있다
2. 시뮬 틱 17 ms 가 이제 최고 배속의 유일한 병목이다

---

# M9.2 해역 도입 (2026-08-28)

관전에서 나온 네 가지를 처리한 기록이다.

1. 반란국 이름 중복
2. 해전이 항상 화면 정중앙에서 일어남
3. 상륙 알림 도배 — 누가 어디로 가는지 모르고 지도도 안 바뀜
4. 제해권 시스템이 뭔지 안 보임

**2·3·4 의 원인은 하나였다.** `FeatureTagger._sea_basins()` 가 바다를 연결 성분으로
플러드필하므로 seed 1 기준 바다 10,000 타일 중 **9,902 타일이 분지 하나**다
(나머지는 2~55 타일짜리 호수 8개). 그래서

- 함대 마커가 전부 그 분지의 무게중심 한 점에 쌓였고,
- 제해권은 "세계 1등 해군이 지구 전체 바다를 독식" 아니면 무주공산이었고
  (M8.5 실측: 섬 소유국이 자기 앞바다를 쥔 비율 1%),
- 그 1등만은 `_neighbors()` 가 붙여 준 간선으로 매 턴 지구 반대편 연안에
  1홉 순간이동했다. 목표는 매 턴 재배정되므로 갔다 왔다만 반복해 로그만 쌓였다.

이번 작업은 **시뮬 수치를 바꾼다.** §3.2 / §10 의 M8.5·M9 회귀 해시는 폐기하고
아래 §16 의 값을 새 기준선으로 삼는다.

## 12. 해역 (`sim/model/sea_zone.gd`, `sim/worldgen/sea_splitter.gd`)

`ProvinceSplitter` 와 같은 다중 소스 동시 확장을 바다에 적용한다. 고도 장벽이 없어
그만큼 단순하다. 분지를 넘는 해역은 만들지 않는다.

```text
TARGET_MIN 60  TARGET_MAX 130  AVG_TARGET 90  SEED_MIN_DIST 6
seed 1 결과: 해역 118개  최소 2  중앙값 80  최대 188  해협 6  RNG 키 "sea_split"
```

고아 타일은 덩어리째 한 해역에 몰아주지 않고 **인접 해역에서 BFS 로 한 칸씩** 흡수한다.
덩어리 흡수는 한 해역만 238타일로 부풀렸다.

해역이 반도를 감싸면 평균 좌표가 육지에 떨어진다. 마커가 땅 위에 뜨지 않도록
`centroid` 는 평균에 가장 가까운 **자기 바다 타일**로 옮긴다.

배선: `Province.sea_basin_ids` → `sea_zone_ids`, `Fleet.basin_id` → `zone_id`,
`Nation.naval_control_basins` → `naval_control_zones`,
`WorldState.basin_provinces` → `SeaZone.coast_provinces`.

## 13. 해군

예산(`ship_cost`/`navy_share`/`target_ships`)은 그대로다. 배치·전투·통제만 바꿨다.

- 함대는 **위치를 갖는 실체**다. 매 턴 함선을 분지 사이로 순간이동시키던
  `_redistribute` 를 없애고, 함대는 해역 그래프 위를 **한 턴에 한 칸** 움직인다.
- 국가당 함대 상한 3. 함대 수는 "다툴 바다" 수로 정하고, 남는 함대는 해체한다.
  죽은 함대는 배열에 쌓지 않고 되살려 쓴다 (300턴 관전에서 함대 배열이 무한히 자란다).
- 제해권: 그 해역 1위가 2위 × 1.15 이상. **함선이 없는 바다는 아무도 쥐지 못한다**
  (예전에는 단독 1척이면 통제권을 가졌다). 해협은 1.35 — 병목은 쉽게 안 넘어간다.
- 전시에 한해 제해권 변동을 기록에 남긴다. 평시 교체까지 흘리면 기록이 그것만 남는다.

성능: 판정마다 전 함대를 훑던 `_is_contested` 를 해역→국가 사전으로 바꾸고,
해역 BFS 를 딕셔너리에서 `PackedInt32Array` 두 개로 바꿔 출발 해역별로 캐시했다.
**4.76 ms → 2.0 ms/턴.**

이때 `_move_fleets` 가 이동 전 상태의 스냅샷을 보게 됐다. 예전에는 국가 순서대로
움직이며 앞 국가의 이동 결과를 뒷 국가가 봤다 — 순서 의존이 사라진 쪽이 옳다.

## 14. 상륙은 두 턴이다

`Army.at_sea_zone` / `landing_target` 추가. 승선한 부대는 `province_id = -1` 이고
`armies_by_province` 에서 빠지므로 교전·공성·주둔 판정이 자동으로 건너뛴다.
바다 위에서는 굶기지 않는다 — 위험은 격침 하나로 충분하다.

1. **승선** — 같은 해역의 연안으로 갈 때, 그 해역 제해권이 있어야 한다 (`embarked`)
2. **상륙** — 다음 턴, 여전히 쥐고 있으면 내린다. 사기 ×0.85 (`amphibious_landing`)
3. **격침** — 그 사이 제해권을 잃었으면 전멸 (`convoy_sunk`).
   설계서 §12.4 "제해권 상실 시 전멸"의 실물이다.

경로도 국지화했다. `_neighbors()` 의 바다 간선은 **같은 해역의 연안**으로만 가고,
`_hops()` 는 바다 한 칸을 2홉(승선+상륙)으로 센다. 승선한 부대는 야전 편성에서 빠져
목표가 바다 위에서 바뀌지 않는다 — 이 재배정 핑퐁이 상륙 도배의 직접 원인이었다.

**`_fronts()` 에 바다 건너 전선을 추가했다.** 예전에는 전선이 육지 인접뿐이라
섬 반란은 진압군이 *목표로 삼지도* 못했다 (M8.5 §2.1 의 "섬 독립률 100%").

## 15. 해상 보급

`compute_supply_field` 의 다익스트라에 **해역 노드**를 넣었다 (id 는 프로빈스 수만큼 밀어 인코딩).

```text
연안 프로빈스 → 통제 해역   +40.0 / sea_supply_mult   (SEA_STEP_COST)
통제 해역 → 인접 통제 해역  +12.0                     (SEA_LINK_COST, 신규)
해역 → 그 해역 연안         +0
```

제해권을 한 해역 쥐면 근해까지, 여러 해역을 이으면 원정 보급이 된다.

## 16. 반란국 이름

`"%s 반란군" % 모국명` 은 같은 모국에서 두 번 봉기하면 이름이 겹치고,
반란국의 반란은 "X 반란군 반란군"이 됐다.

이제 봉기 지역에서 뽑는다: `data/names/*.json` 의 `nation_stems` × 새 `rebel_titles`
(문화별 5개). 프로빈스 id 로 결정론적으로 고르고 이미 쓰인 이름이면 다음 어간으로
넘어간다 — **새 난수를 뽑지 않는다.** 반란 시점에 난수를 뽑으면 그 뒤 모든 시스템의
난수열이 밀린다 (§15). 모국은 기록 줄에만 남는다: `반란  <반란국> ← <모국> · <지역>`.

## 17. 지도

- 새 레이어 **제해권**: 바다 타일을 통제국 색으로 칠하고(물색과 0.72 혼합) 해역
  테두리를 그 나라 색으로 두른다. 육지는 어둡게 깔아 뒤로 물린다.
  알파 0.42 로 얹는 방식은 어두운 물 위에서 화면상 구분이 되지 않았다.
- 바다 타일 삼각형 배열(정점 60,000)은 따로 캐시해 **제해권 모드에서만** 제출한다.
- 함대·해전 마커는 해역 중심에 붙는다. 이제 흩어진다.
- 승선한 부대는 배 마커 + 목적지까지의 선으로 표시한다.
- 바다에 마우스를 올리면 해역 번호·제해권·함대 척수·승선 부대를 띄운다.
- 기록에 `승선`·`상륙`·`수송선단 격침`·`제해권`·`해전` 줄을 추가하고
  해역 링크(`[url=z:..]`)를 누르면 그 바다로 이동한다.
- **치안 분견대의 도하는 기록에 올리지 않는다.** 평시 승선의 대부분이 이것이다
  (seed 1 × 200턴: 승선 79건 중 평시 66건 전부가 `garrison_province >= 0`).
  섬 주둔지는 7턴 주기로 비었다 채워지므로(§19-4) 그대로 흘리면 기록이 그것만 남는다.
  이벤트에는 `"garrison"` 표식만 싣고 판단은 뷰가 한다 — 시뮬 기록은 온전하다.

## 18. 검증

### 결정론 (새 기준선)

M8.5·M9 해시는 폐기한다. 해역·상륙·전선 규칙이 시뮬을 바꿨다.

```text
seed 1 × 300턴  (tools/batch_sim.gd --runs 1 --turns 300 --seed0 1)
sim           296b394eb382a0f4e76eda7ce4e8f0f8ddfac760cd0b10941cbcd4c0b00cf28d
defaults      6fbf15b82f8826c430eb066265529e434f2e6198580f0a75c4d9a2af7435de42
credit events 4d1ca43a88c892532892a53d892a3000351f0d7a23eff355c3a56ed11e15aa8d
```

같은 명령을 두 번 돌려 세 파일 모두 바이트 동일했다. seed 1~3 × 100턴도 동일했다.

### 테스트

```text
market tests: PASS      military tests: PASS
view tests: PASS        war tests: PASS
credit tests: PASS      rebellion tests: PASS
character tests: PASS   naval tests: PASS   (신규)
```

`tools/test_naval.gd` — 해역 분할(개수·최대 크기·연결성·연안 프로빈스 상호 참조·
같은 시드 재현), 승선 2턴, 제해권 상실 시 격침, 제해권 없으면 승선 안 함,
해역 두 칸 원정 보급이 근해보다 나쁨.
`tools/test_rebellion.gd` — 같은 모국의 두 반란국 이름이 다름.
`tools/test_view.gd` — 함대 마커가 한 점에 겹치지 않음, 제해권 지도가 실제로 칠해짐,
반란국이 생긴 뒤에도 국명 유일.

### 밸런스 (6런 × 300턴, `tools/probe_rebellion.gd`)

| 지표 | M8.5 | M9.2 | 목표 |
|---|---|---|---|
| 정부 진압률 | 28% | **47%** | 30~60% |
| 독립 인정률 | 72% | **53%** | 30~60% |
| 섬 반란 독립률 | 100% (44/44) | **57%** (29/51) | — |

M8.5 부터 두 번의 튜닝에서 계속 대역 밖이던 두 지표가 처음으로 안에 들어왔다.
원인은 하나였다 — 육로가 닿지 않는 땅에는 전선 자체가 없었다.

상륙은 seed 1 × 200턴 기준 승선 79 · 상륙 77 · 격침 2 다 (예전에는 로그를 채웠다).

### 성능

```text
시뮬 틱      22.6 ~ 26.5 ms/turn  (M9.1 17 ms)
  Infra 8.3 · Military 5.9 · Diplomacy 3.3 · Supply 3.0 · Naval 2.0 · 나머지 4
뷰 턴당 작업  색 0.37 · 경계 1.65 · 전쟁 0.47 · 패널 0.41 ms
             (제해권 모드에서만 바다 색 2.04 ms — 통제가 바뀐 해역만 칠하면 그보다 싸다)
실렌더 1280×720 (vsync off)
  1배속   266 fps 평균 · 0.25 턴/초
  32배속  157 fps 평균 · 최악 프레임 50.6 ms · 10.66 턴/초
```

관전 체감은 M9.1(32배속 154 fps · 최악 52.4 ms · 10.6 턴/초)과 같다. 늘어난 틱 시간은
해군(2.0 ms)과 살아남은 국가·군대가 늘어난 몫이고, 프레임당 틱 상한 2가 흡수한다.

### 실제 렌더

```text
m9_2_map_political.png  정치 지도 · 흩어진 함대 마커
m9_2_map_naval.png      제해권 레이어
m9_2_transport.png      승선한 부대와 목적지 선
m9_2_log.png            승선·상륙·반란(반란국 ← 모국) 링크
```

## 19. 남은 것

1. 시뮬 틱의 최대 항목은 여전히 `Economy.tick_infra` (8.3 ms) 다. 해군과 무관하다.
2. 본토 반란 진압률이 73% 로 대역 상단에 붙었다 — 바다 건너 전선이 생기면서
   진압군이 늘어난 몫이라 다음 튜닝에서 볼 값이다.
3. 제해권 레이어의 바다 채우기(0.72 혼합)는 확대하면 육지와 헷갈릴 여지가 있다.
4. **섬 주둔지의 7턴 진동.** `GARRISON_NEED_MIN`(0.35) 하나로 배정과 해제를 다 판단해서,
   주둔이 불만을 누르면 곧바로 해제되고 불만이 다시 올라 재배정된다. 프로빈스 214 실측:
   불만 0.10 에 15명 승선 → 상륙 → 3턴 뒤 need 0.03 으로 해제 → 7턴 뒤 반복.
   해제된 분견대는 `_organize` 의 `_merge_smallest` 가 프로빈스를 안 보고 합쳐
   본토로 순간이동한다(M9.2 이전부터의 동작). 고치려면 해제 문턱을 배정 문턱보다
   낮추고(이력), 병합을 같은 프로빈스끼리로 제한해야 한다 — 둘 다 밸런스를 바꾼다.
   지금은 기록에서 감추는 것으로만 대응했다.
